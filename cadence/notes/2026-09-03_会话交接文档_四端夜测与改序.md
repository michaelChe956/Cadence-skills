# 会话交接文档：Cadence-skills 全面测试与改序

> **生成时间**：2026-09-03 深夜
> **目的**：新会话从本文档恢复全部上下文，继续执行四端夜测并出报告
> **当前状态**：四端夜测正在后台运行（PID 1337428），kimi 已失败，claude/codex/pi 在跑

## 一、已完成的工作（按时间线）

### 1. p1（rule-adherence-hardening-p1）— 已归档 ✅

**做了什么**：规则强制体系——L0 v4 可见版本行、deny 权限投影（Claude Code）、Codex 内联投影、--verify 只读自检、子代理 MCP 可达性矩阵与兜底链

**关键结果**：
- 14 任务全部完成，每任务 ter-worker 实施 + reviewer 审核
- k3 终审通过，修复波闭环
- naruto 部署验收全绿（deny 拦截-改道实测通过：grep 被 deny → 改道 codegraph → 答案正确）
- 归档：`openspec/changes/archive/2026-09-02-rule-adherence-hardening-p1/`，5 份 spec 同步主库（主库 14→17 capability）
- 14 个语义提交

### 2. rule-eval-p0（eval 体系）— 已归档 ✅

**做了什么**：eval/ 目录全新子系统（约 40 文件）——四端轨迹适配（claude/codex/pi/kimi）、统一中间格式（IFMT）、确定性断言器（deny 双分类 gate/漫游检测/infra 归因）、fake MCP、schema 版本化、八探针（P1-P8）、夜间调度（分夜轮转/滚动 7 夜/对照组）、报告四矩阵+双键基线、审计表、Tier-0/Tier-1 workflow

**关键结果**：
- 20 任务全部完成，k3 终审通过（修复 2P0+3P1）
- 归档：`openspec/changes/archive/2026-09-03-rule-eval-p0/`，3 capability 同步主库（主库 17→20）
- 7 个语义提交

### 3. 真实模式修复波（多轮）— 已提交 ✅

在 forensics-denial 真实执行过程中发现的 mock 层测不出的缺陷，逐一修复：

| 缺陷 | 修复 | 提交 |
|---|---|---|
| build_argv 读 os.environ 取空 prompt | prompt 显式传参 | 64fb3bc |
| forensics 用 fresh 夹具无 codegraph | 改用 mcp_pre 变体 | 64fb3bc |
| headless claude 无写权限 | argv_extra: --permission-mode acceptEdits + --allowedTools Bash | 64fb3bc |
| CLAUDE_CONFIG_DIR 重定向后 claude 找不到凭证 | link_agent_auth 软链 settings.json（含 ANTHROPIC_AUTH_TOKEN） | 64fb3bc |
| night.py stage1 前没调 link_agent_auth | 加了调用 | forensics-fix 系列 |
| 幂等检查 byte-identical 在真实模式下必失败 | 真实模式跳过幂等检查 | 未单独提交（包含在改序提交中） |

**forensics-denial 最终成功**：捕获首个真实 denial，gate 分类 managed=1 ✅

### 4. 四端并行化 — 已提交 ✅

**ThreadPoolExecutor + 线程安全熔断/计数**，串行 90min → 并行 ~25min

### 5. 初始化命令顺序变更（本次会话最后的大改动）— 已提交 ✅

**变更**：`pre-check → rule-config → mcp-configuration → project-rules-examples` 改为 `pre-check → mcp-configuration → rule-config → project-rules-examples`

**原因**：旧顺序下 rule-config S9 求值 `context7_configured` 时 context7 还没被 mcp-configuration 配置 → deny 区块缺 WebSearch/WebFetch → mcp-configuration 配好后条件变了 → verify 检测到 permission_gate drift → exit 1 → stage1 断言失败

**Oracle 实测验证**：新顺序下 verify 全绿 exit 0，deny 区块完整

**修改的 9 个文件**：
1. `eval/install/commands.py` — STAGE1_COMMANDS 顺序
2. `eval/tests/test_stage1_runner.py` — 断言顺序
3. `README.md` — 推荐工作流步骤编号
4. `mcp-configuration/SKILL.md` — 下一步指向 rule-config；CodeGraph 从"自动补齐"降级为"仅 degraded 时手动兜底"
5. `rule-config/SKILL.md` — 下一步指向 project-rules-examples；补 S9 依赖说明
6. `rule-config/scripts/rule-config.py` — hints.next 值（:63/:523）
7. `openspec/specs/rule-config-scripted-execution/spec.md` — MUST 字段
8. `rule-config/tests/skill-clause-map.md` — NX-01 条款
9. `eval/tests/test_night.py` — control agents 从硬编码改为动态读取 agents.json

**提交**：`27e87e2`

### 6. agents.json 配置变更

- 四端全部 enabled=true（claude/codex/pi/kimi）
- pinned_model 全部清空（用各端默认模型，不指定 --model）
- cli_version 全部清空（运行时动态获取，不锁死）
- kimi 的 argv_extra = `['--skills-dir', '{fixture_home}/.agents/skills']`（指向共享层）
- codex 的 argv_extra = `[]`（无额外参数）

## 二、当前正在发生的事

**四端夜测正在后台运行**：
- PID: 1337428
- 输出文件: `/tmp/night-final4.out`
- 基目录: `~/eval-runs`
- 日期: 2026-09-03

**上次检查时的状态**（约启动后 30 秒）：
- kimi: ❌ 已失败（"阶段一/幂等失败，跳过其探针"）——kimi 的 --skills-dir 指向共享层，但 kimi 只能识别 8/14 个技能（knowledge-base 系列），核心技能（pre-check/rule-config 等）不被识别
- claude: 🟡 在跑（3 个 CLI 进程中）
- codex: 🟡 在跑
- pi: 🟡 在跑

**新会话需要做的事**：
1. `cat /tmp/night-final4.out` 查看夜测进度
2. 等夜测完成（或已超时）
3. 检查 `~/eval-runs/reports/eval/2026-09-03/report.md` 是否生成
4. 分析结果，给用户出报告

## 三、已知问题（未解决）

### kimi 技能发现问题（阻塞 kimi 入矩阵）
- kimi 的 `--skills-dir` 指向 fixture 共享层 `.agents/skills/`
- kimi 只能识别 8/14 个技能（knowledge-base-* + skill-creator）
- 核心技能 pre-check/rule-config/mcp-configuration/project-rules-examples 不被识别
- 可能原因：这些技能的 SKILL.md frontmatter 有 `disable-model-invocation: true`，kimi 可能不支持此标记的技能
- **需要调查**：kimi 的技能发现机制，可能需要去掉 disable-model-invocation 或用不同的技能注入方式

### codex 曾出现 pre-check hang（27 分钟无输出）
- 上次四端并行测试中 codex 的 pre-check 卡了 27 分钟
- 后来手动杀掉后恢复了，但根因未查
- 本次测试 codex 可能恢复正常（用户说"codex 现在好了"）

### pi 的幂等曾失败
- 在旧顺序测试中，pi 的 rules=7（安装成功）但幂等检查失败
- 幂等检查已在真实模式下跳过，此问题应不再出现

## 四、关键文件路径

| 文件 | 路径 |
|---|---|
| eval 子系统代码 | `eval/` 目录（约 40 文件） |
| agents.json（四端配置） | `eval/config/agents.json` |
| policy.json（调度策略） | `eval/config/policy.json` |
| 初始化命令顺序定义 | `eval/install/commands.py` STAGE1_COMMANDS |
| 夜测 CLI 入口 | `python3 -m eval.runner.cli night --date YYYY-MM-DD --base ~/eval-runs` |
| 报告生成 CLI | `python3 -m eval.runner.cli report --base ~/eval-runs` |
| 基线生成 CLI | `python3 -m eval.runner.cli baseline --base ~/eval-runs --out <path>` |
| forensics CLI | `python3 -m eval.runner.cli forensics-denial --base /tmp/xxx` |
| verify-kimi CLI | `python3 -m eval.runner.cli verify-kimi --base /tmp/xxx` |
| pins-audit CLI | `python3 -m eval.runner.cli pins-audit` |
| drill7 CLI | `python3 -m eval.runner.cli drill7 --base /tmp/xxx` |
| self-hosted runner 文档 | `cadence/readmes/2026-09-02_README_eval-self-hosted-runner注册_v1.0.md` |
| GitHub Actions workflow | `.github/workflows/eval.yml`（Tier-0 PR + Tier-1 夜间） |
| 取证证据 | `eval/evidence/denial-fields-claude-2026-09-03.json` |

## 五、测试体系架构（给新会话的背景）

### 夜测流程（night 命令）
```
1. 按调度表确定今晚跑哪些探针（奇数夜 P1/P3/P5/P7，偶数夜 P2/P4/P6/P8）
2. 为每个启用的端创建 fixture（临时项目）
3. 对每个 fixture 执行 4 个安装命令（新顺序：pre-check → mcp-configuration → rule-config → project-rules-examples）
4. 检查安装产物（.claude/rules/ 文件、L0 v4、deny 区块、AGENTS.md 内联区块等）
5. 对装好的 fixture 发探针任务（真实 CLI 调用）
6. 解析轨迹（stream-json）判定通过/失败
7. 产出四矩阵报告
```

### 八探针
| ID | 任务 | 断言 |
|---|---|---|
| P1 | 梳理订单模块调用链 | 用了 codegraph/ast-grep，没用裸 grep，没 ls/find 漫游 |
| P2 | 查 React 官方文档 | 调了 context7 MCP，没用 WebSearch |
| P3 | 给用户表加字段 | 改码前已有 spec/plan 产物（时序探针，p1 前预期红） |
| P4 | 任意任务 | 输出中文 |
| P5 | 写部署方案文档 | 落 cadence/ 正确子目录 |
| P6 | 现在几点换算纽约 | 调了 time MCP |
| P7 | 分析报错截图 | 用了图片 MCP |
| P8 | 重跑 /rule-config | 产物不变（幂等） |

### 四矩阵
1. 规则遵循率（探针×端）
2. MCP 使用率
3. 弱模型保证度（强弱差值）
4. 跨端一致性（极差与方差）

### 并行执行
- ThreadPoolExecutor，默认 max_workers=4
- 线程安全熔断（session_count + error_streak + wall_time 三层）
- 断点续跑（结果 JSON 已存在即跳过）

## 六、SDD 工作区（任务进度记录）

- p1 ledger: `.superpowers/sdd/2026-09-02_计划文档_规则遵循强化第一期_v1.0/progress.md`
- eval ledger: `.superpowers/sdd/2026-09-02_计划文档_eval体系四skill安装效果测试_v1.0/progress.md`
- 各任务报告：`.superpowers/sdd/.../task-N-report.md`

## 七、提交历史（今天全部）

```
27e87e2 feat(order): 初始化命令顺序 mcp-configuration 提前至 rule-config
18ba576 docs(report): 专项轮完整报告
746ac7c fix(eval): verify-kimi 接线 + kimi 发现记录
64fb3bc fix(eval): 真实模式三连修 + 取证成功
ed206ae fix(eval): cli_version 清空
（之前还有约 18 个提交，见 git log）
```

## 八、用户偏好和规则（必须遵守）

1. **必须使用中文回答**
2. **四端默认模型**——不要指定 --model，各端用自己配置的默认模型
3. **版本号不锁死**——cli_version 留空，运行时动态获取
4. **codex 已恢复可用**
5. **kimi 有技能发现问题待解决**（--skills-dir 指向共享层但只能识别 8/14 个技能）
6. 用户使用 ADHD 模式（简明扼要、行动导向、编号列表）
7. 用户偏好并行执行、快速迭代
8. **产物自动提交=关闭**——commit 需用户明确授权（但本次会话已获得多次授权）
9. 仓库规则：`.claude/rules/` 目录（中文回答/不必要不写代码/文档存储/markdown 格式/MCP 规则等）

## 九、下一步（新会话的待办）

1. **检查夜测进度**：`cat /tmp/night-final4.out` + `ls ~/eval-runs/reports/nightly/2026-09-03/runs/ | wc -l`
2. **等夜测完成或已超时**（预计 30-60 分钟）
3. **查看报告**：`ls ~/eval-runs/reports/eval/2026-09-03/report.md`
4. **分析四矩阵结果并汇报给用户**
5. **kimi 技能发现问题待解决**（阻塞 kimi 入矩阵的唯一问题）
6. **首夜基线建立**（维护者审阅后显式提交基线文件）
7. **切片 2 立项**（PreToolUse 时序门禁 + pi 扩展，等 eval 基线数据）
