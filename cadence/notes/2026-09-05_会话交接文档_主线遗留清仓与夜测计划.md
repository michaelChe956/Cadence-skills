# 会话交接文档：pre-check 脚本化收尾 + 主线遗留清仓计划

> **生成时间**：2026-09-05
> **目的**：新会话从本文档恢复全部上下文，先执行阶段 0 清仓，再按主线推进（夜测→codex hang→pi/kimi→PR）
> **当前状态**：pre-check 脚本化实施 8 任务全部闭环（含 whole-branch 终审+OpenSpec 契约补建）；主线 12 项遗留已列计划，阶段 0 细节已定，用户已裁决 0.2
> **上游交接**：`cadence/notes/2026-09-04_会话交接文档_pre-check脚本化实施与环境故障.md`（本会话起点）

## 〇、用户最新决策（最高优先级，必须遵守）

1. **0.2 裁决（已定，不再需要 oracle 推荐）**：O-1 spec 张力采用**执行方案**——四端产物齐全时**必须执行 `openspec update`**（spec 正确，改代码对齐 spec）。即删除/修改 `test_ready_projection_skips_update` 固化的"齐全跳过 update"行为，改为齐全时执行 update（幂等语义：update 后无变化仍可记 skipped，但命令必须发出）。实施时注意与 `openspec/specs/init-skill-sequencing/spec.md:31`（"对缺失客户端 init 后执行 update"）与 :48-51（齐全时 update）合并理解，以"齐全也 update"为准
2. **阶段 0 其余 6 项按已推荐方案执行**（详见第四节）
3. **不 push**：全部主线工作（阶段 0-5）完成后统一建分支提 PR；当前 main 领先 origin/main 45 提交，保持本地
4. 换新会话；交接要求零丢失
5. 持续有效：中文回答、ADHD 模式、独立任务完成即 commit（已授权）、产物自动提交=关闭（CLAUDE.md 项目配置节）

## 一、本会话完成了什么（时间线）

### 1. 环境确认与恢复
- 只读核查：`~/.pi/agent/npm/node_modules/@earendil-works/` 残留软链已清空、fnm 树 pi-server stub 不存在（用户自行修复）
- reviewer 冒烟通过 → 上会话"sol-worker 代行审查"的 ruling **解除**，恢复 reviewer 审查

### 2. T4-T8 全部完成（subagent-driven-development 流程）
- **T4 软链同步**：上会话 fix round 1 半落盘（Important-2 落地、Important-1 def 行丢失）被 re-review 抓出 → fix round 2 补落盘 → APPROVE。教训已入 ledger：**re-review 一律以工作树新鲜证据为准，subagent 报告陈述不作完成依据**
- **T5 verify+失败快返**：APPROVE_WITH_NITS→修复（死代码/形状断言/注释/报告计数）→APPROVE
- **T6 SKILL 三步契约**：440→69 行，全文仅剩 `pwd -P` 与契约单命令两处可执行文本；禁用短语清单旧文组 14 项+补充组 3 项（两组自证：旧文组对 HEAD 旧版逐一命中、新版全不命中）；smoke 27→26 系删恒真 16c 检查
- **T7 eval 联动**：首轮 REJECT（ter-worker 引入重复类遮蔽吞 4 回归测试 133→131，reporter 未贴全量计数掩盖）→修复→APPROVE（136）；实施自行加第五断言 pre-check.home 追认合规
- **T8 集成验收**：离线段（集成脚本+10 负向矩阵+3 fixture 补建+基线采集器兼容修复）→ REJECT（集成脚本失败传播断裂=永绿假防线）→修复+红态演示→APPROVE；Step 5 真机验收详见下
- 每任务：task-brief→ter-worker 实施→controller 手写 review package（git diff+新文件）→reviewer 审查→修复循环→commit→ledger 完成行

### 3. Step 5 真实 claude 验收（用户批准+Plan 命令勘误）
- **Plan Step 5 命令三处缺陷（oracle 发现）**：①`stream-json` 在 `-p` 下必须配 `--verbose`；②空隔离 HOME 无凭证且 settings.json 含 `enabledPlugins`/`extraKnownMarketplaces` 会拉 marketplace 旧版 459 行 SKILL.md 造成遮蔽→copy-and-strip settings+显式软链工作区新 skill；③本机无 `/usr/bin/time`→bash 内建 time。验收线不变。Plan 正文已加勘误标注（提交 92027a7）
- **结果（后端 glm-5.3-flash[1M]，与 6m38s 基线同源可比）**：
  - 一跑（冷启动）：118.9s（脚本侧 18s+模型侧 ~101s）/2 工具调用/overall=success/openspec init created=1/git clone/links created=56
  - 二跑（幂等）：81.3s/2 调用/五 phase 全 skipped/目标树 diff 空
  - 防遮蔽：旧版特征（heredoc/digraph/快速参考）零命中；模型行为完美贴三步契约
  - **冷启动余量仅 1.1s**——结论限定该后端，夜测需监控分位
- **执行事故（已还原，如实记录）**：二跑首次漏 `cd "$ISO/project"`，claude 在真实仓库根执行→openspec 门控 update 改写 20 投影文件+init 出 `.agents/`、`.kimi-code/` 两目录→`git checkout -- .claude .pi` + 确认内容后 `rm -rf .agents .kimi-code` 完整还原并复核零残留。教训：①验收命令必须单块原子化含 cd ②多 pathspec checkout 遇未跟踪目录整条 fatal 中止，需分开执行

### 4. whole-branch 终审（oracle）→ REJECT → 修复 → 复核 APPROVE
- **Critical-1**：T1 基线 tree.txt 记录的是**虚构目录形态**（`.pi/skills/brainstorm.md` 等旧命名，OpenSpec CLI 从不产出）；T8 为迁就它加 `legacy_five_files_ready` 放宽 pi/kimi 判定→实测旧命名项目 `overall=success` 但投影缺失=活跃 MUST spec 假绿
- 修复：删 legacy 函数+恢复严格判定（pi 恰 5 目录+5 文件/kimi 恰 5 目录，三方对齐 spec/eval/设计）+TDD 双测试（旧命名必须 init 且 verify 不放行，实测 run/check 均 failed 无误杀）+基线 **precheck-v1 作废→修正 fixture 为真实 openspec 布局+27e87e2 旧脚本+本地 bare remote 重采 precheck-v2**（冻结脚本 SHA 与独立计算一致，防覆盖守卫对 v2 生效）+B-1 v2 README 网络口径勘误
- Important-1 config.yaml 缺失中文提示恢复（spec:21 MUST，T6 收敛时失落）；Important-4 采集器注入 CADENCE_TEST_GIT_CANDIDATES 本地 bare remote 纵深防御；Important-3 Plan 48 步全勾选
- 提交 e809695、60fa485

### 5. OpenSpec 契约补建（用户选补建而非豁免）
- 新能力 spec `pre-check-scripted-execution`（6 Requirements/14 Scenarios：五阶段 phases[] 九字段+Git 四字段+source_entries/layers[] **平铺**/三步 SKILL 契约/统一 ≤120s+240s 熔断/冲突语义/幂等 v2 基线/verify 只读+失败快返）
- 流程：openspec new change→artifacts（proposal/design/tasks，tasks 8/8 [x] 注明提交区间含两端）→validate --strict→sync 主 spec→archive `2026-09-05-pre-check-scripted-execution`
- 保真度审查 REJECT（spec 把 source_entries/layers[] 误写成 `links.` 嵌套路径，实现是平铺）→修正（含幂等 Scenario 可观测化、区间标注）→APPROVE
- ADDED-only 不触碰既有 spec；openspec validate --all --strict **21 passed**；提交 f9f67c1

### 6. finishing 收尾
- 新鲜证据：pre-check test.sh 3 pass 0 fail（config.yaml 提示 MUST 实测在案）/ eval Ran 136 OK / openspec 21 passed / 工作树干净
- 用户决定：不 push，全部完成后统一 PR

## 二、提交历史（本会话全部，main 直连）

```
f9f67c1 feat(openspec): 追溯补建 pre-check-scripted-execution 契约
60fa485 docs(plan): 终审后勾选全部 48 步
e809695 fix(pre-check): whole-branch 终审修复（假绿路径铲除+基线 v2 重采+config.yaml 提示恢复）
92027a7 docs(plan): T8 Step5 勘误标注
a743705 feat(pre-check): T8 集成验收与负向矩阵
f3de89e test(eval): T7 pre-check 断言联动
748f309 docs(pre-check): T6 SKILL.md 440→69 行收敛为三步契约
04b6918 feat(pre-check): T5 verify 阶段与失败快返
4f6f7db docs(note): T4 中断环境故障会话交接文档
a4a73e8 feat(pre-check): T4 do_superpowers_links 阶段
（上会话：eefb2ea T3 / 5bf81ce T2 / 2572384 T1 / a05c74d 立项 / dc0c5c5 eval 修复）
```

## 三、产物一致性口径（用户问过，新会话可能再问）

| 层面 | 一致性 |
|---|---|
| 落盘产物（安装/四端投影/56 软链/Git 状态） | **1:1 零差别**——基线 precheck-v2 用旧脚本冻结，集成腿每次跑 1:1 diff 为空才过；防覆盖守卫 exit 3 |
| stdout JSON | steps[] 保留原样 + 新增 phases[]（五阶段审计，这是交付物不是回归） |
| stderr 中文摘要/退出码语义 | 一致 |

## 四、后续计划（阶段 0-5，用户已批框架+0.2 已裁决）

### 阶段 0：小修清仓（夜测前置，1 会话内，分两批并行派 ter-worker）

| # | 项 | 做法（已定） | 量级 |
|---|---|---|---|
| 0.1 | agents.json 行尾换行 | 补回 dc0c5c5 丢失的文件末尾换行 | 5min |
| 0.2 | **O-1 spec 张力（用户已裁决：执行方案）** | 四端齐全时**必须执行 openspec update**：改 pre-check.sh do_openspec_phase 齐全分支（当前跳过，见 test_ready_projection_skips_update 及 pre-check.sh:311-313 注释 :295），改/删该测试并新增"齐全时 update 被调用且无变化仍幂等 skipped"测试；注意与幂等语义调和（命令发出=updated 计数口径需定，建议：update 实际无变更仍记 skipped） | ~1h |
| 0.3 | codegraph telemetry 幂等 | TDD：真实 HOME 二跑断言排除 `~/.codegraph/telemetry-queue.jsonl`（codegraph version 每跑追加一行）；落在 eval 真实模式断言与/或集成快照排除清单 | 30min |
| 0.4 | eval 真实模式空断言修复 | `pre-check.home`/`pre-check.links` 当前快照 fixture HOME 而脚本写真实 HOME=永远通过的假防线；改为断言脚本实际操作的 HOME 路径（stage1.py 真实模式 home=None 时的采集重定向） | 1-2h |
| 0.5 | mock action 口径失真 | mock_cli.py 的 action 值（phase-check）对齐真实脚本（do_base_tools/verify-ready/init-update-verify/fetch-pull-ff-only/all-skipped） | 30min |
| 0.6 | HUP/INT/TERM 清理测试 | 起真实子进程 kill -HUP/-INT/-TERM，断言临时报告文件被删（现只测 EXIT 路径） | 30min |
| 0.7 | 采集器 git≥2.22 校验 | capture-baseline.sh 启动时检查 git 版本，不满足打印明确错误 | 15min |

- 第一批：0.3+0.4（直接决定夜测数据可信度）；第二批：0.1/0.5/0.6/0.7 并行；0.2 独立第三批（行为变化需谨慎审查）
- 每批：ter-worker 实施（TDD 先失败测试）→ reviewer 审查 → commit → ledger
- **0.2 注意**：这是行为变化，实施前重读 `openspec/specs/init-skill-sequencing/spec.md` :31 与 :48-51 全文，若两处仍有歧义以用户裁决"齐全也 update"为准；eval mock（openspec phase skipped=4）与断言若受影响同步改

### 阶段 1：夜测恢复（核心主线，1-2 晚后台）
1. claude 单端先行健康对照（1 晚）：验证脚本化后稳定 ≤120s、冷启动分位（余量 1.1s 需样本量）、二跑幂等
2. 绿后四端全量夜测，建首夜基线
3. 观测：duration 分位/tool_calls/INFRA_FAIL 率（night.py 分层 timeout 240s 熔断已就绪）

### 阶段 2：codex pre-check hang 根因（依赖阶段 1 数据）
- 不复发→根因=模型临场编程，close 记 ledger
- 复发→bisect：codex CLI 进程树 vs 技能发现层；派 scout 取证+oracle 定位

### 阶段 3：pi/kimi 技能发现层（可与阶段 2 并行）
1. 派 scout 摸 pi-coding-agent 技能发现机制事实（只认 `.pi/skills`+`~/.pi/agent/skills`？有无共享层配置）——注意 pi 文档在 `/home/michaelche/.local/share/fnm/node-versions/v24.17.0/installation/lib/node_modules/@earendil-works/pi-coding-agent/{README.md,docs}`
2. 方案二选一（oracle 推荐+用户拍板）：上游修 vs 安装布局规避（pre-check 给 pi 落 `.pi/skills` 投影副本）
3. kimi 端同法核实（交接文档只有 pi 有明确根因：pi 不扫 `~/.agents/skills` 共享层→收到 /rule-config 凭记忆写旧版 v3 产物）
4. 若选安装布局方案→达 OpenSpec 阈值，走 brainstorming→OpenSpec

### 阶段 4：环境缺口（穿插，均需用户点头——动全局环境）
1. shellcheck 安装后补跑全量静态检查
2. macOS Bash 3.2 真机验证（用户若有 mac，跑一次 `cd cadence-init/skills/pre-check/tests && bash test.sh`）

### 阶段 5：收尾提 PR
1. 全量回归三件套：pre-check test.sh（smoke+Python+集成）/ `PYTHONPATH=. python3 -m unittest discover -s eval/tests -p 'test_*.py'` / `openspec validate --all --strict`
2. 从 origin/main 建分支→push→PR（用户审）

## 五、关键文件路径

| 文件 | 路径 |
|---|---|
| SDD ledger（进度真源，gitignored 但在盘） | `.superpowers/sdd/2026-09-04_计划文档_pre-check执行脚本化实施_v1.0/progress.md` |
| 实施 Plan（48 步全勾+Step5 勘误） | `cadence/plans/2026-09-04_计划文档_pre-check执行脚本化实施_v1.0.md` |
| 设计文档 | `cadence/designs/2026-09-04_design_pre-check执行脚本化_v1.0.md` |
| 新能力主 spec | `openspec/specs/pre-check-scripted-execution/spec.md` |
| 归档 change | `openspec/changes/archive/2026-09-05-pre-check-scripted-execution/` |
| 被改造脚本 | `cadence-init/skills/pre-check/scripts/pre-check.sh`（~1100 行） |
| SKILL.md（69 行三步契约） | `cadence-init/skills/pre-check/SKILL.md` |
| 测试入口 | `cadence-init/skills/pre-check/tests/test.sh`（3 腿：smoke 26+Python 39+集成） |
| 基线 v2（旧脚本冻结+守卫） | `cadence-init/skills/pre-check/tests/baselines/precheck-v2/` |
| 各任务报告/review package | `.superpowers/sdd/2026-09-04_.../task-N-*.md`、`whole-branch-fix1-report.md`、`openspec-backfill-report.md` |
| task-brief 脚本 | `/home/michaelche/.pi/agent/skills/subagent-driven-development/scripts/task-brief` |
| /tmp 真机验收现场（可弃） | `/tmp/precheck-real-claude.SUIWF5/`（含 transcript 双份+快照） |

## 六、subagent 使用规则（用户既定+本会话新增）

**既定**：
1. 检索代码一律 scout[user]；代码勘察派 scout，controller 只做证据整合与决策编排
2. 做决策优先 oracle[user]（不可用降级 my-openai/gpt-5.6-sol），oracle 只给推荐、用户拍板
3. 调研 researcher；编 plan 优先 sol-worker（**传输故障史：要求增量写盘**）
4. 任务默认 ter-worker[user] 执行、reviewer[user] 审核；测试任务优先 ter-worker
5. 尽量并行（注意共享文件冲突：tests 目录/skill-clause-map 并行必撞，曾因此串行 T5-T8）
6. subagent 环境出问题只报告用户，禁止自行修复
7. 项目内自行决策（重要节点与 oracle 商量）；项目外（环境/全局）先告知用户

**本会话新增（过程教训）**：
8. **oracle 是 Anthropic 模型，fork 上下文会因签名 thinking 块 400**——派 oracle 必须 `context: "fresh"`（自给足文件路径）
9. ter-worker 会把报告写错位置（曾写进 cadence/plans/ 新建目录）——派工时给绝对路径；报告统一在 `.superpowers/sdd/<plan>/task-N-report.md`
10. 504 网关超时≠子代理失败：先核对文件是否已改，再决定重派（本会话一次 504 实际已跑完）
11. reviewer 审查必须要求全量测试计数逐模块对账贴原文（T7 曾用局部计数掩盖 133→131 流失）
12. 审查包（review package）工作树未提交时由 controller 手写：git diff+新文件全文+声称摘要；subagent 报告声称不作为完成依据

## 七、遗留问题全量清单（12 项=3 主线+7 转主线+2 环境）

主线（阶段 1-3）：夜测恢复 / codex hang 根因 / pi+kimi 技能发现层
转主线（阶段 0 清 7 项中的 0.1/0.3/0.4/0.5/0.6/0.7 + 0.2 已裁决待实施）
环境（阶段 4）：shellcheck / macOS Bash 3.2 真机
观感级（可带病上线，不列任务）：base-tools result=skipped 但 skipped=0+action 回退函数名 / 冷启动余量 1.1s（夜测监控）

**已排除/已解决**（勿重复处理）：subagent 环境（已恢复）；基线虚构形态（v2 重采）；config.yaml 提示（已恢复）；集成假绿防线（已修+红态验证）；eval 重复类遮蔽（已修）；OpenSpec 契约缺失（已补建归档）；Plan 勾选（48/48）

## 八、新会话执行要求

1. 开工先读：本文档 + ledger（progress.md 尾部）+ `git log --oneline -12` + `git status`（确认干净）
2. 顺序：阶段 0 第一批（0.3+0.4）→ 第二批（0.1/0.5/0.6/0.7 并行）→ 0.2（用户已裁决执行方案，独立实施）→ 阶段 1 夜测（claude 先行）→ …
3. 沿用 SDD 流程：task-brief→ter-worker→手写 review package→reviewer→修复循环→commit→ledger
4. 每 2-3 项向用户 ADHD 表格汇报
5. 重要裁决记 ledger Ruling 行；本文件第四节即计划真源，完成后逐项更新状态
