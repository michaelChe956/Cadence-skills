# omp 第五端支持与考场五端化 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 oh-my-pi(omp)成为第五个受支持客户端:规则经共享 frontmatter 与 `.agents/rules` 软链桥渐进加载,`.omp/AGENTS.md` 受管活引用,L0 升 v5,eval Docker 夜测考场整体五端化。

**Architecture:** 规则物理源保持 `.claude/rules/`(RF-05 权威覆盖管线不动),模板增加共享 frontmatter(`description`/`paths`/`alwaysApply` 各端单字段消费;`agents:` 为可选能力字段,框架模板不使用);rule-config 新增 `s11_omp_bridge` 步骤管理 `.agents/rules/` 文件级相对软链集合与 `.omp/AGENTS.md` 整文件受管活引用;eval 侧 `container.py`/`session.py` 以 kimi 同模式(ELF 复制+凭据最小集)接入 omp,既有 P1-P8 探针与透视五端化。

**Tech Stack:** Python 3(rule-config.py 197KB 脚本,PyYAML)、Markdown(SKILL.md/merge-semantics.md/模板)、Bash(verify-managed-lifecycle.sh)、Python eval 框架(eval/docker、eval/install、eval/scoring)、podman 容器(Arch 镜像)。

**Spec:** `openspec/changes/support-omp-client/`(proposal/design/specs/tasks);设计文档 `cadence/designs/2026-09-09_方案设计_五端渐进式规则架构_v1.0.md`;证据 `cadence/analysis-docs/2026-09-09_调研证据_五端规则与context发现机制/`。

## Global Constraints

- 本仓根 `AGENTS.md`/`CLAUDE.md`/`.claude/rules/` 为受管文件,本次不改其内容(框架自用规则另行演进,不在本 change)。
- 所有文档中文;Markdown 遵循 `.claude/rules/markdown-format.md`。
- 产物自动提交开关:关闭——Plan 不含 git commit 步骤;验证命令代替提交门禁。(注:执行期若用户打开开关,由执行者按仓内风格提交,提交信息前缀 `feat(rule-config):`/`feat(eval):`/`docs(rule-config):`。)
- rule-config 失败关闭契约:任何失败分支先写报告再退出,退出码语义(SKILL.md 失败关闭表)不变;受管文件写入必须 `atomic_write`,备份走 `backup_file`→`cadence/legacy/<14位时间戳[-N]>/`。
- 受管规则文件绝不执行章节合并;模板与落地文件逐字一致(RF-05)。
- omp 实测锚定(18.1.14):仅 `.agents/rules` 文件级软链可用;`paths: []` 在 Claude 仍强载(永不匹配 glob 才是行为路由桶);frontmatter 各端未知字段静默忽略。
- PRUNE_DIRS 与 SKILL.md find 块必须逐项一致(`assert_bounded_source_scan_contract` 双向断言)。
- 每个 Task 结束跑该 Task 的验证命令;Task 6 与 Task 8 含全量回归。

---

### Task 1: 规则模板共享 frontmatter(10 个源模板 / 8 个落地规则 + 快照测试)

**Files:**
- Modify: `cadence-init/skills/rule-config/references/rules/{language,code-usage-coding,code-usage-noncoding,code-reading-coding,code-reading-noncoding,document-storage,markdown-format,mcp-servers,openspec-superpowers-workflow,playwright}.md`(10 个源模板;落地名仍为 7+1 固定集)
- Test: `cadence-init/skills/rule-config/tests/test_rule_config.py`(模板快照断言)

**Interfaces:**
- Consumes: 现有 `_load_reference`/`TEMPLATE_REQUIRED`/`CANONICAL_RULE_*`(rule-config.py 243/433/328 行区域)。
- Produces: 带下述 frontmatter 的模板;Task 3 的 `render_omp_agents_md` 与 Change B 的 `rules.frontmatter` 断言消费同一契约。

- [ ] **Step 1: 按分桶写入 frontmatter(每模板文件头插入,`---` 围栏)**

常驻桶 `language.md`:

```yaml
---
description: 语言规则——所有响应必须使用中文
alwaysApply: true
---
```

条件桶 `code-usage-coding.md` / `code-reading-coding.md`(coding 落地源;glob=rule-config.py `SOURCE_EXTS`(~424-427,13 个扩展名)口径 + `tsx`/`jsx` 增补,共 15 条):

```yaml
---
description: 代码使用规则——TDD 与代码规范(coding)
paths:
  - "**/*.rs"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.py"
  - "**/*.go"
  - "**/*.java"
  - "**/*.c"
  - "**/*.cpp"
  - "**/*.cs"
  - "**/*.swift"
  - "**/*.kt"
  - "**/*.php"
  - "**/*.rb"
---
```

(`code-reading-coding.md` 的 description 换为「代码阅读规则——CodeGraph 与 ast-grep 优先(coding)」。)

条件桶(noncoding 落地源)与行为路由桶用永不匹配 glob(意图注释置于 frontmatter 后正文首行之前,以 HTML 注释):
```yaml
---
description: 代码使用规则——非编码项目行为规范(non-coding)
paths:
  # 行为路由桶占位:本规则经 L0 路由必读,不依赖路径自动注入
  - "**/*.zz-cadence-behavioral"
---
```

`code-reading-noncoding.md` 同法(description「代码阅读规则——文档与配置的结构化定向阅读(non-coding)」)。

`document-storage.md`:`paths: ["**/*.md", "cadence/**"]`,description「文档存储规则——Cadence 产物目录与命名」。
`markdown-format.md`:`paths: ["**/*.md"]`,description「Markdown 格式规则——代码块嵌套与格式」。
`playwright.md`:`paths: ["web/**", "e2e/**", "**/*.spec.ts", "**/*.test.ts"]`,description「Playwright CLI 使用规范」。

媒体触发桶 `mcp-servers.md`:

```yaml
---
description: MCP Server 与工具使用规则——联网、图片、浏览器自动化
paths:
  # 媒体触发:触图自动载;web-search/浏览器场景经 L0 路由必读
  - "**/*.png"
  - "**/*.jpg"
  - "**/*.jpeg"
  - "**/*.webp"
  - "**/*.gif"
  - "**/*.svg"
---
```

行为路由桶 `openspec-superpowers-workflow.md`(不设 `agents`——框架不携带 agent 限定,omp 子代理默认可见全部规则并自行按需读取,2026-09-10 用户修正):

```yaml
---
description: OpenSpec×Superpowers 协作流程——会话路由与阶段门禁
paths:
  # 行为路由桶占位:本规则经 L0 首行必读,不依赖路径自动注入
  - "**/*.zz-cadence-behavioral"
---
```

`README.md` 模板不加 frontmatter(目录页常驻,不桥接)。

- [ ] **Step 2: 快照测试更新**

`tests/test_rule_config.py` 中模板快照断言(搜 `TEMPLATE_REQUIRED` 相关用例)更新期望内容;新增断言:每个框架模板含 frontmatter 围栏、`description` 非空、桶字段组合符合上表(language 无 paths 且 alwaysApply:true;行为路由桶不设 agents(框架不携带 agent 限定);其余必有非空 paths)。

```python
def test_rule_templates_frontmatter_contract():
    import re
    from rule_config_scripts import _load_reference  # 按仓内测试既有导入路径
    for name in ("language.md", "code-usage-coding.md", "code-usage-noncoding.md",
                 "code-reading-coding.md", "code-reading-noncoding.md",
                 "document-storage.md", "markdown-format.md", "mcp-servers.md",
                 "playwright.md", "openspec-superpowers-workflow.md"):
        text = _load_reference(f"rules/{name}")
        m = re.match(r"^---\n(.*?)\n---\n", text, re.S)
        assert m, f"{name} 缺 frontmatter"
        fm = m.group(1)
        assert "description:" in fm, name
        if name == "language.md":
            assert "alwaysApply: true" in fm and "paths:" not in fm
        elif name == "openspec-superpowers-workflow.md":
            assert "agents:" in fm and "main" in fm and "zz-cadence-behavioral" in text
        else:
            assert re.search(r"paths:\n\s+- \"", fm), name
```

(导入路径以仓内测试文件既有写法为准——`tests/` 已有对 `references/` 的读取用例,沿用其导入方式。)

- [ ] **Step 3: 验证**

```bash
cd /home/michaelche/workspace/github/Cadence-skills
python3 -m pytest cadence-init/skills/rule-config/tests/test_rule_config.py -k "template" -x -q
```

预期:模板相关用例全 PASS。

---

### Task 2: L0 内核 v5(omp 客户端语义)

**Files:**
- Modify: `cadence-init/skills/rule-config/references/rules/agent-routing-kernel.md`
- Create: `cadence-init/skills/rule-config/references/rules/l0-history/agent-routing-kernel-v4.md`(现 v4 全文归档)
- Modify: `cadence-init/skills/rule-config/scripts/rule-config.py`(L0 版本常量,搜 `openspec-superpowers-routing:v` 标记构造处)
- Modify: `eval/install/assertions.py`(L0 断言层升 v5:~177 `L0_BEGIN` 常量、~321 断言名 `l0.v4`、~343-344 `v3.upgraded` 文案)
- Modify: `eval/tests/test_stage1_assertions.py`(fixture 内嵌 v4 标记字面量 ~19-23 同步 v5)
- Modify: `cadence-init/skills/rule-config/references/merge-semantics.md`(§4 L0 全节版本表述 v2→v5:~113 行「当前 L0 版本为 **v2**」及 L0-01/L0-04 行的版本引用)
- Test: `cadence-init/skills/rule-config/tests/test_rule_config.py`(L0 升级路径用例);`eval/tests/test_stage1_assertions.py`

**Interfaces:**
- Consumes: 现有 v4 内核模板与 L0 版本升级机制(`l0_block`/`classify_l1`/受管标记对);assertions.py 既有 `L0_BEGIN` 常量与 `assert_stage1`。
- Produces: v5 规范源;eval 断言 `l0.v5`(由 `l0.v4` 改名,无并行旧名);Task 6 回归与 Change B 消费。

- [ ] **Step 1: 归档 v4 → 修改内核为 v5**

`cp agent-routing-kernel.md l0-history/agent-routing-kernel-v4.md`;然后内核文件:

1. 标记对 `cadence-managed:openspec-superpowers-routing:v4` → `v5`(首尾两处);
2. 首行可见版本 `Cadence L0 路由内核 v4` → `v5`;
3. Skill 调用语义行(第 5 行)替换为:

```markdown
Skill 调用：Claude/Kimi 原生调用；Codex/pi/omp 清单选择后将用途并入首段回执，并立即全文读取对应 SKILL.md（omp 经 `skill://`，Codex/pi 直接读文件）。首段输出路由回执；Skill 调用后才读仓库规则或用仓库工具。
```

其余内容逐字保留。

- [ ] **Step 2: 脚本版本常量与升级路径**

`rule-config.py` 中 L0 版本常量/标记(搜 `v4` 构造)升 `v5`;升级路径表加入 `v4 → v5`(与既有 v3→v4 同一确定性升级分支;`l0-history` 加载名单补 v4)。

- [ ] **Step 3: eval 断言层同步升 v5(不留旧断言名并行)**

`eval/install/assertions.py`:`L0_BEGIN` 常量中 `:v4:` → `:v5:`(或改为由 `L0_CURRENT_VERSION` 构造消除双源);断言名 `l0.v4` → `l0.v5`;`v3.upgraded` 断言文案改为「v3 区块未升级为 v5」;**~352 行正则中硬编码的结束标记 `v4:end -->` 改为由版本构造(或 `L0_END` 常量),~3 行 docstring「L0 v4 标记」同步**;`eval/tests/test_stage1_assertions.py` fixture 中 v4 标记字面量同步 v5。此步防止「mock 单测因 fixture 与常量同步陈旧而假绿」的结构性漂移。

- [ ] **Step 4: 测试**

L0 升级用例新增:v4 成对标记且逐字一致 → 升 v5 不经决策;v4 漂移 → 权威覆盖归档;入口恰含一个 v5 区块。

```python
def test_l0_v4_deterministic_upgrade_to_v5(tmp_path):
    # 复用既有 L0 升级测试的 fixture 构造方式(见 test_rule_config.py 中 v3→v4 用例)
    # 前置:入口写入 l0-history/agent-routing-kernel-v4.md 全文(含 v4 标记对)
    # 断言:apply 后入口恰含一个 v5 区块,区块外内容逐字保留
    ...
```

- [ ] **Step 5: 验证**

```bash
python3 -m pytest cadence-init/skills/rule-config/tests/test_rule_config.py -k "l0" -x -q
python3 -m pytest eval/tests/test_stage1_assertions.py -x -q
```

---

### Task 3: `s11_omp_bridge` 步骤实现

**Files:**
- Modify: `cadence-init/skills/rule-config/scripts/rule-config.py`(STEP 常量区 ~80-103、STEP_FUNCS ~3902、plan 构建区 ~2195、新 step 函数置于 step_s10 之后 ~3853-3900)
- Test: `cadence-init/skills/rule-config/tests/test_rule_config.py`

**Interfaces:**
- Consumes: `STEP_CODEX_INLINE` 同款注册模式;`atomic_write`/`backup_file`/`_safe_read`/`ensure_parent`;`report.setdefault("warnings", [])` 通道。
- Produces: `step_s11_omp_bridge(root, intents, plan, report)`;常量 `OMP_CONTEXT_MARKER = "<!-- cadence-managed:omp-context:v1 -->"`、`OMP_AGENTS_MD_BODY`;warning code `OMP_BRIDGE_MATERIALIZED`、`OMP_BRIDGE_USER_FILE_KEPT`。

- [ ] **Step 1: 常量与注册**

```python
STEP_OMP_BRIDGE = "s11_omp_bridge"
# STEP_ORDER 尾部追加 STEP_OMP_BRIDGE;STEP_FUNCS 追加映射
OMP_CONTEXT_MARKER = "<!-- cadence-managed:omp-context:v1 -->"
OMP_AGENTS_MD_BODY = (
    f"{OMP_CONTEXT_MARKER}\n"
    "@../.claude/CLAUDE.md\n"
    "@../AGENTS.md\n"
)
OMP_BRIDGE_EXCLUDE = {"README.md"}
WARNING_OMP_MATERIALIZED = "OMP_BRIDGE_MATERIALIZED"
WARNING_OMP_USER_FILE = "OMP_BRIDGE_USER_FILE_KEPT"
```

plan 构建区仿 s10(2195-2216):dry-run 计算动作预览(创建/修复/清理/幂等),不写入。

- [ ] **Step 2: 软链集合管理核心函数**

```python
def _bridge_actions(rules_dir: Path, bridge_dir: Path) -> tuple:
    """计算桥接动作与 warnings。源=.claude/rules/*.md 减 OMP_BRIDGE_EXCLUDE。
    返回 (actions, warns):actions=[(op, name) ...],op∈create/repair/remove/
    conflict-backup;warns=[{"code": ..., "detail": ...} ...](仓内 warnings dict 形态)。
    repair 仅针对既有软链;同名非软链普通文件走 conflict-backup(归档后替换)。"""
    sources = ({p.name for p in rules_dir.glob("*.md")}
               - OMP_BRIDGE_EXCLUDE) if rules_dir.is_dir() else set()
    actions: list = []
    warns: list = []
    if not bridge_dir.exists():
        return [(("create", n)) for n in sorted(sources)], warns
    entries = {p.name: p for p in bridge_dir.iterdir()}
    for name in sorted(sources):
        p = entries.get(name)
        want = f"../../.claude/rules/{name}"
        if p is None:
            actions.append(("create", name))
        elif p.is_symlink():
            if os.readlink(p) != want:
                actions.append(("repair", name))
        else:
            # 与规则同名的普通用户文件:归档后替换,绝不无备份删除
            actions.append(("conflict-backup", name))
            warns.append({"code": WARNING_OMP_USER_FILE,
                          "detail": f".agents/rules/{name} 同名普通文件,归档后替换"})
    for name, p in sorted(entries.items()):
        if name in sources:
            continue
        if p.is_symlink():
            target = os.readlink(p)
            if ".claude/rules/" in target:
                actions.append(("remove", name))  # 孤儿软链(源已退役)
                continue
        warns.append({"code": WARNING_OMP_USER_FILE,
                      "detail": f".agents/rules/{name} 非管线文件,保留"})
    return actions, warns


def _apply_bridge_action(root: Path, bridge_dir: Path, op: str,
                         name: str) -> bool:
    """执行单个动作;返回是否走了物化降级。conflict-backup 先 backup_file 归档。"""
    bridge_dir.mkdir(parents=True, exist_ok=True)
    target = bridge_dir / name
    if op == "remove":
        target.unlink(missing_ok=True)
        return False
    if target.exists() or target.is_symlink():
        if op == "conflict-backup":
            backup_file(target, root)  # 归档到 cadence/legacy,可回滚
        target.unlink()
    try:
        os.symlink(f"../../.claude/rules/{name}", target)
        return False
    except OSError:
        src = bridge_dir.parent / ".claude" / "rules" / name
        atomic_write(target, _safe_read(src) or "")
        return True
```

(warnings 挂到 `report.setdefault("warnings", [])` 由 step 函数完成,与仓内既有 dict 形态一致——见 rule-config.py ~2834 `warnings.append({...})` 写法。)

- [ ] **Step 3: step 函数与 `.omp/AGENTS.md` 受管**

```python
def step_s11_omp_bridge(root: Path, intents: Intents, plan: dict,
                        report: dict) -> None:
    """S11 执行:.agents/rules 软链桥 + .omp/AGENTS.md 受管活引用。"""
    rules_dir = root / ".claude" / "rules"
    bridge_dir = root / ".agents" / "rules"
    actions, warns = _bridge_actions(rules_dir, bridge_dir)
    actions_log: list = []
    materialized = False
    for op, name in actions:
        if _apply_bridge_action(root, bridge_dir, op, name):
            materialized = True
        actions_log.append({"path": f".agents/rules/{name}", "action": op})
    if materialized:
        warns.append({"code": WARNING_OMP_MATERIALIZED,
                      "detail": "symlink 不可用,已物化副本;重跑可刷新,注意漂移"})
    report.setdefault("warnings", []).extend(warns)
    omp_path = root / ".omp" / "AGENTS.md"
    existing = _safe_read(omp_path)
    if existing is not None and existing != OMP_AGENTS_MD_BODY:
        backup_file(omp_path, root)  # 归档旧文件(含手工过渡版)至 cadence/legacy
        report.setdefault("backups", []).append({"file": str(omp_path)})
    if existing != OMP_AGENTS_MD_BODY:
        ensure_parent(omp_path)
        atomic_write(omp_path, OMP_AGENTS_MD_BODY)
        actions_log.append({"path": ".omp/AGENTS.md", "action": "managed-write"})
    report.setdefault("steps", {})[STEP_OMP_BRIDGE] = {
        "status": "ok", "actions": actions_log}
```


- [ ] **Step 4: 单测(先写失败测试再实现)**

用例清单(每个独立 tmp_path fixture):

```python
def test_s11_creates_symlinks_for_all_rules_except_readme(tmp_path): ...
def test_s11_idempotent_second_run_no_write(tmp_path): ...
def test_s11_repairs_broken_or_mistargeted_symlink(tmp_path): ...
def test_s11_removes_orphan_pointing_to_retired_rule(tmp_path): ...
def test_s11_keeps_user_file_with_warning(tmp_path): ...
def test_s11_materializes_when_symlink_unavailable(tmp_path, monkeypatch): ...
    # monkeypatch os.symlink 抛 OSError;断言副本内容==源、warning OMP_BRIDGE_MATERIALIZED
def test_s11_replaces_manual_omp_agents_md_with_backup(tmp_path): ...
    # 预置 aria 式过渡文件;断言内容==OMP_AGENTS_MD_BODY 且 cadence/legacy 有归档
def test_s11_creates_omp_agents_md_when_missing(tmp_path): ...
def test_s11_conflict_same_name_regular_file_backed_up_then_replaced(tmp_path): ...
    # 预置 .agents/rules/code-usage.md 普通文件;断言 cadence/legacy 有归档、软链替换、warning
```

- [ ] **Step 5: 验证**

```bash
python3 -m pytest cadence-init/skills/rule-config/tests/test_rule_config.py -k "s11 or bridge or omp" -x -q
```

---

### Task 4: PRUNE_DIRS、SKILL.md、merge-semantics、clause-map

**Files:**
- Modify: `cadence-init/skills/rule-config/scripts/rule-config.py`(PRUNE_DIRS ~400)
- Modify: `cadence-init/skills/rule-config/SKILL.md`(有界扫描 find 块 ~81-87;概述/warnings/下一步表述)
- Modify: `cadence-init/skills/rule-config/references/merge-semantics.md`(新 §:OR-01~07)
- Modify: `cadence-init/skills/rule-config/tests/skill-clause-map.md`(对账)

**Interfaces:**
- Consumes: `assert_bounded_source_scan_contract` 双向断言(tests/verify-managed-lifecycle.sh)。
- Produces: PRUNE_DIRS 含 `.omp`;OR 表七行;SKILL.md 编排含 s11 与 warnings code。

- [ ] **Step 1: PRUNE_DIRS 在 `.pi",` 行后插入 `".omp",`;SKILL.md find 块剪枝目录同步加 `-o -name .omp`**

- [ ] **Step 2: merge-semantics.md 新增 §(编在 §7 可选规则增量之后)**

| 行 ID | 资产 | 冲突状态 | 两模式动作 | 备份 | 报告 |
|---|---|---|---|---|---|
| OR-01 | `.agents/rules/` 软链 | 应有软链缺失 | 创建(相对路径 `../../.claude/rules/<名>`) | 无 | `bridge-create` |
| OR-02 | 既有软链 | 断链/指向非规则源 | 重定向修复 | 无 | `bridge-repair` |
| OR-03 | 孤儿软链 | 指向已退役规则 | 删除 | 无 | `bridge-remove` |
| OR-04 | 非管线文件 | `.agents/rules/` 内用户文件 | 逐字保留 | 无 | warning `OMP_BRIDGE_USER_FILE_KEPT` |
| OR-05 | symlink 不可用 | `os.symlink` 失败 | 物化副本;重跑刷新 | 无 | warning `OMP_BRIDGE_MATERIALIZED` |
| OR-06 | `.omp/AGENTS.md` | 缺失/非受管内容/受管不一致 | 整文件写为受管活引用;旧内容备份 | 复制归档 `cadence/legacy/` | `managed-write`+backups |
| OR-07 | 与规则同名的非软链普通文件 | `.agents/rules/<规则名>.md` 为用户普通文件 | 复制归档 `cadence/legacy/` 后以软链替换 | 归档原文件 | warning `OMP_BRIDGE_USER_FILE_KEPT`+归档路径 |

- [ ] **Step 3: SKILL.md 概述补 s11 一句、warnings code 清单补两条;skill-clause-map.md 加七行对账(OR-01~07)**

- [ ] **Step 4: 验证(双向断言 + 文档一致性 grep)**

```bash
cd /home/michaelche/workspace/github/Cadence-skills
bash cadence-init/skills/rule-config/tests/verify-managed-lifecycle.sh 2>&1 | tail -3
grep -c '"\.omp"' cadence-init/skills/rule-config/scripts/rule-config.py
grep -c "name \.omp" cadence-init/skills/rule-config/SKILL.md
grep -c "OR-0[1-7]" cadence-init/skills/rule-config/references/merge-semantics.md
```

预期:harness PASS;三处 grep 各 ≥1。

---

### Task 5: verify 子命令 omp 资产断言

**Files:**
- Modify: `cadence-init/skills/rule-config/scripts/rule-config.py`(`run_verify` ~4209)
- Test: `cadence-init/skills/rule-config/tests/test_rule_config.py`

**Interfaces:**
- Consumes: `run_verify` 既有五项自检结构(只读、退出码 0/1)。
- Produces: verify 第 6 项「omp 资产」:软链集合一致 + `.omp/AGENTS.md`==`OMP_AGENTS_MD_BODY`;漂移→退出码 1 并在报告 `verify` 节列差异明细。

- [ ] **Step 1: 失败测试(fixture:删一个软链 → verify rc=1;补回+改 `.omp/AGENTS.md` → rc=1;全对 → rc=0)**

- [ ] **Step 2: 实现断言项并入 verify 报告 JSON(`steps`/`checks` 按现有结构扩展)**

- [ ] **Step 3: 验证**

```bash
python3 -m pytest cadence-init/skills/rule-config/tests/test_rule_config.py -k "verify" -x -q
```

---

### Task 6: rule-config 全量回归

**Files:** 无新增(验证性 Task)

- [ ] **Step 1: 全量单测**

```bash
cd /home/michaelche/workspace/github/Cadence-skills
python3 -m pytest cadence-init/skills/rule-config/tests/test_rule_config.py -x -q
```

- [ ] **Step 2: 生命周期 harness**

```bash
bash cadence-init/skills/rule-config/tests/verify-managed-lifecycle.sh 2>&1 | tail -5
```

- [ ] **Step 3: eval 侧既有套件不回归(本 Task 只读验证)**

```bash
python3 -m pytest eval/tests -x -q
```

预期:三组全绿。任何红→回修对应 Task,不带病前进。

---

### Task 7: omp 容器化(A2)

**Files:**
- Modify: `eval/docker/container.py`(cli_map ~99-106、`_copy_auth` ~150-187)
- Test: 手动容器冒烟(本 Task 验证命令)

**Interfaces:**
- Consumes: `Container.exec/copy_in`;宿主 `~/.local/bin/omp`、`~/.omp/agent/{models.yml,config.yml}`。
- Produces: 五端考场容器能力;Task 8 的夜测跑 omp 依赖此。

- [ ] **Step 1: cli_map 增 omp(ELF 复制,同 kimi 分支)**

```python
    if agent == "omp":
        omp_bin = Path.home() / ".local/bin/omp"
        c.copy_in(str(omp_bin), "/usr/local/bin/omp")
        c.exec("chmod +x /usr/local/bin/omp")
```

- [ ] **Step 2: `_copy_auth` 增 omp 最小集**

```python
        "omp": [
            (home / ".omp/agent/models.yml", "/home/tester/.omp/agent/models.yml"),
            (home / ".omp/agent/config.yml", "/home/tester/.omp/agent/config.yml"),
        ],
```

- [ ] **Step 3: 容器冒烟(首启状态写入行为一并验证)**

```bash
cd /home/michaelche/workspace/github/Cadence-skills
python3 - <<'EOF'
from eval.docker.container import create_test_container
import time
c = create_test_container("omp", f"smoke-{int(time.time())}")
r = c.exec("omp -p --model smol '只回复 OK'", timeout=240)
print("rc:", r["rc"]); print(r["stdout"][-200:]); print(r["stderr"][-200:])
c.destroy()
EOF
```

预期:rc=0 且 stdout 含「OK」。若首启因 HOME 状态目录失败:在 `create_test_container` omp 分支追加 `c.exec("mkdir -p ~/.omp/agent")` 后重试;若仍失败→启用降级路径(Task 8 Step 4),并在报告注明。

---

### Task 8: 夜测五端化与透视(A3)

**Files:**
- Modify: `eval/docker/session.py`(`_cli_command` ~212-219、`_PERMISSION_FLAG` ~224-229)
- Modify: `eval/docker/report_matrix.py`(AGENTS ~15)
- Modify: `README.md`(四端→五端表述;透视说明)
- Test: 夜测运行取证(本 Task 验证命令)

**Interfaces:**
- Consumes: Task 1-6(框架落地)、Task 7(omp 容器);`night.py run_docker_night(agent, probe_ids)` 既有流。
- Produces: 五端夜测与透视;omp 基线首轮。

- [ ] **Step 1: session.py 两表增 omp**

```python
        "omp": "omp -p",
    # ...
    "omp": "",
```

- [ ] **Step 2: report_matrix.py `AGENTS = ("claude", "codex", "pi", "kimi", "omp")`**

- [ ] **Step 3: omp 夜测跑 stage1+P1-P8(依赖 Task 1-6 已并入本工作树)**

注意:`night.py` 的 `__main__` 只消费单个逗号分隔探针参数(`sys.argv[2].split(",")`),命令 MUST 用逗号连接:

```bash
python3 -m eval.docker.night omp P1,P2,P3,P4,P5,P6,P7,P8 2>&1 | tail -5
```

预期:probe_summary JSON 输出 8 个探针;结果文件落 `eval/results/docker-night/reports/nightly/<date>/runs/docker-omp-*.json`(8 条)。首轮 omp 成功率允许低于四端(全盲问题已由框架解决,失败集中在模型遵循率;逐 probe 记录,不判 Task 红)。

- [ ] **Step 4: 降级路径与机器可读载体**

实现:omp 考场失败(容器化/无头)时,`night.py` 仍为 omp 落一个 schema 1.0 run 记录并携带 `"degrade": "<原因>"` 字段;`report_matrix.py` 检测到 `degrade` 字段时在矩阵后打印降级注记行(如 `> omp 考场降级:<原因>`)。

验证:临时将 `cli_map` omp 分支指向不存在路径,跑一次 `python3 -m eval.docker.night omp P1`;断言:①四端矩阵照常可出透视;②omp run JSON 含 `degrade` 字段;③透视输出含降级注记。恢复后重跑 Step 3 抽查 P1(验证后工作树恢复原状)。

- [ ] **Step 5: 透视输出与基线首轮**

```bash
python3 eval/docker/report_matrix.py --day all | head -20
```

确认矩阵含 omp 列;把 omp 首轮各 probe 成功率写入 `eval/baselines/baseline.json` 的 `probe_agent` 对应条目(仅新增 omp 键,不动四端键)。

- [ ] **Step 6: README 同步 + 取证归档**

README 中「四端 24/24」类表述更新为五端口径(附 omp 首轮数据);透视输出与夜测运行记录摘要有言必录,供 Change B 与后续回归引用。

---

## Self-Review 结论

- Spec 覆盖:omp-client-support 5 需求 ↔ Task 3/4/5/7;managed-rule-lifecycle delta(frontmatter/五端等价)↔ Task 1/2;progressive-context-routing delta(v5)↔ Task 2;eval-ci-matrix delta(考场五端化)↔ Task 7/8。无缺口。
- 类型/命名一致:`step_s11_omp_bridge`、`OMP_AGENTS_MD_BODY`、warning code、`"omp": "omp -p"`、AGENTS 元组——各 Task 间引用一致。
- 占位符:无 TBD;测试以「复用仓内既有用例形态」处均指名参照对象。
