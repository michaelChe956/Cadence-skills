# 产物自动提交开关扩展至 code 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 开关改名为“产物自动提交（design/plan/code）”，旧名行身份迁移保值，L0 v3 开关句同步。

**Architecture:** 脚本新增 `TOGGLE_PREFIX_LEGACY` 旧名前缀常量，`_ensure_commit_toggle` 匹配扩为新旧双前缀、规范输出恒为新名；归并/告警逻辑零改动。模板与文档文案同步替换。

**Tech Stack:** Python 3（unittest）、Markdown 模板。

**Spec:** `openspec/changes/extend-artifact-toggle-to-code/`（proposal / specs / design / tasks）

## Global Constraints

- 禁止 `git commit`（本仓库开关为关闭）。
- `_ensure_commit_toggle` 的取值/冲突判定/去重/落位/告警码（`INVALID_TOGGLE`）逻辑零改动，只允许扩展行匹配与输出前缀。
- 新名规范行：`- **产物自动提交（design/plan/code）**：<值>`；旧名 `- **产物自动提交（design/plan）**：` 仅作输入识别。
- L0 v3 模板改文案后 `wc -c` 仍 ≤2560；模板与 `ARTIFACT_PATH_OVERRIDE_TABLE` 的三源逐字一致性不受影响（覆盖表不含开关句）。
- 测试命令：`python3 -m unittest tests.test_rule_config`（本机无 pytest）；harness：`bash tests/verify-managed-lifecycle.sh`（基线 pass=98 fail=0）。

---

### Task 1: 失败测试（RED）+ 基准改新名

**Files:**
- Test: `cadence-init/skills/rule-config/tests/test_rule_config.py`

**Interfaces:**
- Produces: 迁移用例类（新用例）；既有开关用例改为新名断言（对未改脚本失败=预期）。

- [ ] **Step 1: 既有开关用例基准改新名**

将 tests 中所有开关行字面量 `- **产物自动提交（design/plan）**` 替换为 `- **产物自动提交（design/plan/code）**`（约 1366/1373/1380/1437/1438/1445/3171 行及同类），断言期望同步改新名。注意：这些用例构造的输入也应为新名行（模拟新态），旧名输入场景由 Step 2 新用例覆盖。

- [ ] **Step 2: 新增迁移用例（新类 TestToggleLegacyMigration）**

```python
class TestToggleLegacyMigration(unittest.TestCase):
    def test_legacy_prefix_migrates_and_preserves_on(self):
        """ut-toggle-migrate-on：旧名开启行迁移为新名并保留 开启。"""
        text = "## 项目配置\n\n- **产物自动提交（design/plan）**：开启\n"
        out, warns = rc._ensure_commit_toggle(text, "CLAUDE.md")
        self.assertIn("- **产物自动提交（design/plan/code）**：开启", out)
        self.assertNotIn("design/plan）", out.replace("design/plan/code）", ""))

    def test_legacy_prefix_migrates_and_preserves_off(self):
        """ut-toggle-migrate-off：旧名关闭行迁移为新名并保留 关闭。"""
        text = "## 项目配置\n\n- **产物自动提交（design/plan）**：关闭\n"
        out, _ = rc._ensure_commit_toggle(text, "AGENTS.md")
        self.assertIn("- **产物自动提交（design/plan/code）**：关闭", out)

    def test_legacy_invalid_value_preserved_with_warning(self):
        """ut-toggle-migrate-invalid：旧名非法值迁移为新名、保留原文并 warning。"""
        text = "## 项目配置\n\n- **产物自动提交（design/plan）**：也许\n"
        out, warns = rc._ensure_commit_toggle(text, "CLAUDE.md")
        self.assertIn("- **产物自动提交（design/plan/code）**：也许", out)
        self.assertTrue(any(w.get("code") == "INVALID_TOGGLE" for w in warns))

    def test_legacy_and_current_coexist_dedup(self):
        """ut-toggle-migrate-dedup：旧名+新名并存归并为恰好一行新名，取首个值。"""
        text = ("## 项目配置\n\n- **产物自动提交（design/plan）**：开启\n"
                "- **产物自动提交（design/plan/code）**：关闭\n")
        out, _ = rc._ensure_commit_toggle(text, "CLAUDE.md")
        self.assertEqual(out.count("产物自动提交"), 1)
        self.assertIn("- **产物自动提交（design/plan/code）**：开启", out)

    def test_no_toggle_writes_default_off(self):
        """ut-toggle-migrate-default：无开关行写入默认 关闭（新名）。"""
        out, _ = rc._ensure_commit_toggle("# X\n", "CLAUDE.md")
        self.assertIn("- **产物自动提交（design/plan/code）**：关闭", out)
```

说明：并存去重“取首个值”遵循既有重复开关行“保留首个”语义（design D1）；`rc` 为测试文件顶部既有的模块引用（与 TestL0V3Migration 相同方式导入）。若 `_ensure_commit_toggle` 返回签名与上述不符，以脚本实际签名为准调整（读 `scripts/rule-config.py:2472` 附近确认）。

- [ ] **Step 3: 运行确认失败**

```bash
cd cadence-init/skills/rule-config && python3 -m unittest tests.test_rule_config -v 2>&1 | tail -15
```

预期：既有开关用例（新名断言）与全部迁移用例 FAIL（脚本只认旧名）；其余用例 PASS。

### Task 2: 脚本双前缀识别（GREEN）

**Files:**
- Modify: `cadence-init/skills/rule-config/scripts/rule-config.py:2468-2469`（常量区）与 `_ensure_commit_toggle` 匹配处（约 2483-2541 行）

**Interfaces:**
- Consumes: Task 1 测试。
- Produces: `TOGGLE_PREFIX_LEGACY` 常量；`_ensure_commit_toggle` 兼容双前缀。

- [ ] **Step 1: 最小实现**

```python
TOGGLE_PREFIX = "- **产物自动提交（design/plan/code）**："
TOGGLE_PREFIX_LEGACY = "- **产物自动提交（design/plan）**："
```

`_ensure_commit_toggle` 内所有 `line.startswith(TOGGLE_PREFIX)` 改为 `line.startswith(TOGGLE_PREFIX) or line.startswith(TOGGLE_PREFIX_LEGACY)`；取值处 `line[len(TOGGLE_PREFIX):]` 改为按实际命中的前缀截取（用 `line.split("**：", 1)[1].strip()` 或等价方式，保持仅精确前缀行命中）；规范输出/写入处统一用 `TOGGLE_PREFIX`（新名）。不改取值合法性判定（`开启`/`关闭`）、冲突/非法告警、章节归并、去重逻辑。

- [ ] **Step 2: 运行确认全绿**

```bash
cd cadence-init/skills/rule-config && python3 -m unittest tests.test_rule_config 2>&1 | tail -3
```

预期：Ran 219+，OK。若 `BASE_CLAUDE_MD`/`BASE_AGENTS_MD` 等入口基础模板或 harness 中出现旧名字面量，一并改新名基准。

### Task 3: 模板与文档文案同步

**Files:**
- Modify: `cadence-init/skills/rule-config/references/rules/agent-routing-kernel.md`（开关句）
- Modify: `cadence-init/skills/rule-config/references/rules/document-storage.md`、rule-config `SKILL.md`、`references/merge-semantics.md`（开关引用）
- Modify: `.claude/rules/...`、本仓库 `CLAUDE.md`/`AGENTS.md` 中开关相关文案（如有）

- [ ] **Step 1: L0 v3 开关句替换（design D2）**

旧句：“产物自动提交开关：写 design/plan 后读取入口“产物自动提交（design/plan）”，`关闭` 时禁止 `git commit`、只汇报路径；CLAUDE.md 为准、不一致按 `关闭`。”
新句：“产物自动提交开关：完成 design/plan 文档或实现类产物（代码、测试、配置）写入后读取入口“产物自动提交（design/plan/code）”开关，`关闭` 时禁止 `git commit`、只汇报路径；CLAUDE.md 为准、不一致按 `关闭`。”

替换后 `wc -c references/rules/agent-routing-kernel.md` ≤2560（实测若超限，先把新句中“（代码、测试、配置）”缩为“（代码/测试/配置）”再验）。

- [ ] **Step 2: 文档同步**

上述三个文档中“产物自动提交（design/plan）”字面量统一替换为新名（逐文件 grep 确认无残留）；`document-storage.md` 若含开关句语义描述，同步补“实现类产物”表述。

- [ ] **Step 3: 全量验证**

```bash
cd cadence-init/skills/rule-config && python3 -m unittest tests.test_rule_config && bash tests/verify-managed-lifecycle.sh
```

预期：单测全绿、harness pass=98 fail=0。若 harness 样本含旧开关名，改新名基准。

### Task 4: 本仓库 apply 实测

**Files:** 无代码文件；产出验证记录。

- [ ] **Step 1: dry-run + apply**（fake codegraph PATH 隔离，同 Task 4 of 上一计划）：本仓库入口原本无开关行 → apply 后两入口含新名默认 `关闭`；L0 区块与新模板逐字一致；区块外不动。
- [ ] **Step 2: 迁移场景实测**：临时将本仓库 CLAUDE.md 开关行改回旧名 `design/plan）：开启`，重跑 dry-run（预期报告将迁移）+ apply（预期新名 `开启`），验证后把值恢复 `关闭` 再 apply 一次确认收敛（或直接手工改回并在报告记录）。
- [ ] **Step 3: 完整套件并记录输出**：单测 + harness 全绿。

---

## Self-Review

- Spec 覆盖：governance delta（迁移保值/并存归并/非法值/默认值）→ Task 1 Step 2 四个用例 + 既有用例基准；读取层扩展（design/plan/code）→ Task 3 模板文案（行为层约束在 L0 内核句，代码层无读取逻辑）✓
- 占位符：无 ✓
- 命名一致：`TOGGLE_PREFIX_LEGACY`、TestToggleLegacyMigration 各任务一致 ✓
