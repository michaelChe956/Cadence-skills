# 删除实体的矩阵与图表示（墓碑保留制）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans.

**Goal:** 落地墓碑保留制：`已失效` 证据状态枚举 + 墓碑行/节点规范，使 update 删除实体后矩阵/图/等值校验自洽。

**Spec:** `openspec/changes/kb-deleted-entity-tombstone/`；设计 `cadence/designs/2026-09-21_方案设计_删除实体的矩阵与图表示_v1.0.md`。

**Global Constraints:** 禁止 git commit；四文件小改；每步 grep 验证；Tier-0 回归。

---

### Task 1: 模板与 base-info 门禁（WP1）

**Files:**
- Modify: `cadence-init/skills/knowledge-base-base-info/assets/traceability-matrix-template.md`
- Modify: `cadence-init/skills/knowledge-base-base-info/SKILL.md`（§8 门禁 1 与门禁 4）

- [ ] **Step 1: 模板规则 2 枚举扩充**

定位规则 2 原文：
```markdown
2. `证据状态` 只允许 `已确认`、`来源冲突`、`待确认`。
```
替换为：
```markdown
2. `证据状态` 只允许 `已确认`、`来源冲突`、`待确认`、`已失效`；`已失效` 仅允许由 knowledge-base-update 的删除流程产生，初始化阶段不得使用。
```

- [ ] **Step 2: 模板新增规则 6（墓碑行规范）**

在规则 5 之后追加：
```markdown
6. 墓碑行（删除实体）：实体被 Update 删除后，其稳定 ID 保持原样（禁止"（已删除）"等任何注记），历史行原位保留、关系类型不变，证据状态写 `已失效`，关系证据列可追加删除提交（如 `删除提交 <sha>`）。
```

- [ ] **Step 3: base-info §8 门禁 1 枚举同步**

定位门禁 1 原文：
```markdown
1. 关系行一律按 `assets/traceability-matrix-template.md` 的六列结构写入，证据状态只允许 `已确认`、`来源冲突`、`待确认`。
```
替换为：
```markdown
1. 关系行一律按 `assets/traceability-matrix-template.md` 的六列结构写入，证据状态只允许 `已确认`、`来源冲突`、`待确认`；`已失效` 仅由 knowledge-base-update 删除流程产生，base-info 与其他初始化领域不得写入。
```

- [ ] **Step 4: base-info §8 门禁 4 图契约墓碑语义**

定位门禁 4 中 nodes 描述：
```markdown
图固定含 `derived_from: evidence/traceability-matrix.md`、`nodes`（id/kind/label/status/doc 引用）与 `edges`（type/from/to/evidence 证据位置）
```
替换为：
```markdown
图固定含 `derived_from: evidence/traceability-matrix.md`、`nodes`（id/kind/label/status/doc 引用；被 Update 删除的实体保留墓碑节点：id/kind/label/doc 不变、`status: deleted`、可加 `deleted_evidence`）与 `edges`（type/from/to/evidence 证据位置；指向已删除实体的失效边保留，可加 `invalid: true`）
```

- [ ] **Step 5: 验证**

grep：模板规则 2/6 含"已失效"与"墓碑"；门禁 1 含"仅由 knowledge-base-update"；门禁 4 含"status: deleted"。

### Task 2: update skill 删除条目展开（WP2）

**Files:**
- Modify: `cadence-init/skills/knowledge-base-update/SKILL.md`（§4 实体清单"- 删除实体"行）

- [ ] **Step 1: 展开为五条墓碑规则**

定位原文：
```markdown
- 删除实体：从活动清单移除，保留删除证据、旧关系和历史。
```
替换为：
```markdown
- 删除实体（墓碑保留制）：从活动清单移除，按以下规则保留历史——
  1. 稳定 ID 保持原样，禁止附加"（已删除）"等任何注记；
  2. 追溯矩阵历史行原位保留，关系类型不变，证据状态写 `已失效`，关系证据列可追加删除提交；
  3. `evidence/relation-graph.yaml` 保留墓碑节点（id/kind/label/doc 不变，`status: deleted`，可加 `deleted_evidence`）及全部失效边（可加 `invalid: true`）；
  4. 删除证据（提交、差异位置）登记到受影响实体文档与变更历史；
  5. 等值校验口径不变：图边数等于矩阵全部数据行数（含已失效行）、图节点集包含矩阵两端（含墓碑节点）。
```

- [ ] **Step 2: 验证**

grep：update SKILL.md 含"墓碑保留制"与"status: deleted"；五条齐全。

### Task 3: 回归与 codex P3 复验（WP3）

- [ ] **Step 1: Tier-0 三件套回归**

`tier0.py --skill-src`（模板列数一致性自动覆盖新增规则行）、`fixture_check.py`、`selftest.py` 全绿。

- [ ] **Step 2: codex P3 复验**

`python3 evals/kb/run.py --agent codex --variant full --no-build --model deepseek-flash --restore-kb /opt/repo/evals/kb/results/2026-09-20/codex-full/knowledge-base --probes P3`

断言预期：矩阵历史行 ID 无注记、证据状态含 `已失效`、图含墓碑节点、`图节点⊇矩阵两端` 通过 → P3 PASS。若 codex 另有偏差则记录为独立发现，不放宽断言。

- [ ] **Step 3: 汇报**
