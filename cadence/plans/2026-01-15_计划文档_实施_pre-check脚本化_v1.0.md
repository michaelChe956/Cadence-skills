# pre-check 脚本化实施 Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 pre-check 六个基础工具（npx/uvx/ast-grep/codegraph/openspec/pi-mcp-adapter）的检查/安装/验证从 SKILL.md 移交单一 bash 脚本，支持可切换镜像源、增量秒跳过与 opt-in 升级，模型只读 JSON 报告。

**Architecture:** 新增 `scripts/pre-check.sh`（mac/Linux 兼容 bash）+ `scripts/mirrors/{default,cn}.sh`（环境变量源配置）。脚本端到端完成六工具探测/安装/复验，stdout 输出 JSON、stderr 输出彩色摘要；SKILL.md 改为"调脚本 → 读报告 → 处理 Superpowers 软链/OpenSpec 三客户端/Playwright/API Key 四项"。

**Tech Stack:** bash（POSIX 兼容，mac bash 3.2 + BSD 工具 / Linux GNU 工具）、JSON 报告、OpenSpec change `add-pre-check-shell-scripts`。

**关联契约：** OpenSpec change `openspec/changes/add-pre-check-shell-scripts/`（proposal/design/specs/tasks 已获批）。

## Global Constraints

- 产品实现改动范围仅限 `cadence-init/skills/pre-check/`；允许同步维护本 Change 的 `openspec/changes/add-pre-check-shell-scripts/` 契约、tasks 与本 Plan。禁止触碰 `cadence-workflow/`、`rule-config/`、`mcp-configuration/` 与根 `AGENTS.md` / `CLAUDE.md`。
- 脚本必须使用 `#!/usr/bin/env bash` 与 POSIX 兼容语法；禁止关联数组、`grep -P`、`sed -i` 原地差异等 GNU/BSD 分歧特性；禁止引入 Python/Node 运行时依赖。
- 脚本只处理六个工具：npx、uvx、ast-grep、codegraph、openspec、pi-mcp-adapter。Superpowers 软链、OpenSpec 三客户端产物、Playwright、API Key 四项由 SKILL.md 处理，脚本不做。
- 镜像经环境变量注入（命令级 `--registry=` / uv 索引环境变量）；禁止写 `~/.npmrc`、`~/.config/uv/uv.toml`、`git config --global`。
- 升级范围仅限 ast-grep/codegraph/openspec（npm 系）+ uv 本体；pi-mcp-adapter、uvx 临时包、playwright-cli 一律不升级。
- 升级仅在携带 `--upgrade` 时触发；默认不查远端 latest。`--mirror cn` 以 npmmirror/清华 latest 为准，默认以 npmjs/pypi latest 为准；不跨源比对。
- `default.sh` 源值：`CADENCE_NPM_REGISTRY=https://registry.npmjs.org`、`CADENCE_PY_INDEX=https://pypi.org/simple`、`CADENCE_SUPERPOWERS_GIT=https://github.com/obra/superpowers`。
- `cn.sh` 源值：`CADENCE_NPM_REGISTRY=https://registry.npmmirror.com`、`CADENCE_PY_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple`、`CADENCE_SUPERPOWERS_GIT=https://gitee.com/michaelChe-World/superpowers.git`。
- JSON `overall` 三态：success/partial/failed；`steps[].status` 枚举：ready/installed/upgraded/skipped/failed。
- 每个 Task 结束后单独 commit，提交信息风格沿用仓库（如 `feat(cadence-init): ...`），使用中文描述。
- 编辑使用精确字符串替换；`old` 文本必须在原文件中唯一。

---

### Task 1: 镜像源配置文件 default.sh 与 cn.sh（工作包 1）

**Files:**
- Create: `cadence-init/skills/pre-check/scripts/mirrors/default.sh`
- Create: `cadence-init/skills/pre-check/scripts/mirrors/cn.sh`

**Interfaces:**
- Consumes: 无
- Produces: 环境变量 `CADENCE_NPM_REGISTRY`、`CADENCE_PY_INDEX`、`CADENCE_SUPERPOWERS_GIT`（供 Task 2 主脚本 `source` 注入；`CADENCE_SUPERPOWERS_GIT` 供 Task 7 SKILL.md Superpowers 步骤读取）

- [ ] **Step 1: 创建 mirrors 目录并写 default.sh**

```bash
mkdir -p cadence-init/skills/pre-check/scripts/mirrors
cat > cadence-init/skills/pre-check/scripts/mirrors/default.sh << 'EOF'
# Cadence pre-check 镜像源配置：通用官方源
# 由 pre-check.sh 通过 --mirror default（默认）加载，仅在脚本进程内生效。
# 不写入用户全局 npm/uv/git 配置。

# npm registry（影响 npx/npm 全局安装与 npx 临时包）
CADENCE_NPM_REGISTRY="https://registry.npmjs.org"

# Python 包索引（影响 uv/uvx 安装与 uvx 临时包）
CADENCE_PY_INDEX="https://pypi.org/simple"

# Superpowers Git 远端地址（SKILL.md 的 Superpowers 步骤 clone/pull 使用）
CADENCE_SUPERPOWERS_GIT="https://github.com/obra/superpowers"

export CADENCE_NPM_REGISTRY CADENCE_PY_INDEX CADENCE_SUPERPOWERS_GIT
EOF
```

- [ ] **Step 2: 写 cn.sh（大陆镜像）**

```bash
cat > cadence-init/skills/pre-check/scripts/mirrors/cn.sh << 'EOF'
# Cadence pre-check 镜像源配置：中国大陆镜像
# 由 pre-check.sh 通过 --mirror cn 加载，仅在脚本进程内生效。
# 不写入用户全局 npm/uv/git 配置。
#
# 注意：CADENCE_SUPERPOWERS_GIT 指向用户自行同步维护的国内 Git 镜像，
# 需定期与上游 https://github.com/obra/superpowers 同步。

# npm registry（淘宝镜像，影响 npx/npm 全局安装与 npx 临时包）
CADENCE_NPM_REGISTRY="https://registry.npmmirror.com"

# Python 包索引（清华镜像，影响 uv/uvx 安装与 uvx 临时包）
CADENCE_PY_INDEX="https://pypi.tuna.tsinghua.edu.cn/simple"

# Superpowers Git 国内镜像（用户自行同步）
CADENCE_SUPERPOWERS_GIT="https://gitee.com/michaelChe-World/superpowers.git"

export CADENCE_NPM_REGISTRY CADENCE_PY_INDEX CADENCE_SUPERPOWERS_GIT
EOF
```

- [ ] **Step 3: 验证两个文件可 source 且变量正确**

```bash
cd cadence-init/skills/pre-check/scripts
( . ./mirrors/default.sh; echo "default npm=$CADENCE_NPM_REGISTRY py=$CADENCE_PY_INDEX git=$CADENCE_SUPERPOWERS_GIT" )
( . ./mirrors/cn.sh;      echo "cn      npm=$CADENCE_NPM_REGISTRY py=$CADENCE_PY_INDEX git=$CADENCE_SUPERPOWERS_GIT" )
```

Expected:
- default 行输出 `npm=https://registry.npmjs.org py=https://pypi.org/simple git=https://github.com/obra/superpowers`
- cn 行输出 `npm=https://registry.npmmirror.com py=https://pypi.tuna.tsinghua.edu.cn/simple git=https://gitee.com/michaelChe-World/superpowers.git`
- 子 shell（括号）执行，变量不污染当前会话

- [ ] **Step 4: Commit**

```bash
git add cadence-init/skills/pre-check/scripts/mirrors/default.sh cadence-init/skills/pre-check/scripts/mirrors/cn.sh
git commit -m "feat(cadence-init): 新增 pre-check 镜像源配置 default/cn"
```

### Task 2: 主脚本框架——参数解析、mirror 加载、命令级源注入（工作包 2）

**Files:**
- Create: `cadence-init/skills/pre-check/scripts/pre-check.sh`

**Interfaces:**
- Consumes: Task 1 的 `mirrors/<name>.sh`（`CADENCE_NPM_REGISTRY`/`CADENCE_PY_INDEX`/`CADENCE_SUPERPOWERS_GIT`）
- Produces: 全局变量 `MODE`（run/check）、`MIRROR`、`NO_INTERRUPT`（0/1）、`UPGRADE`（0/1）；函数 `load_mirror <name>`、`npm_registry_args`（输出 `--registry=...`）、`uv_index_env`（输出 `UV_INDEX_URL=...`）；JSON 累积数组 `STEPS_JSON`。供 Task 3/4/5 使用。

- [ ] **Step 1: 创建脚本骨架（shebang、安全选项、变量、参数解析、mirror 加载）**

写入 `cadence-init/skills/pre-check/scripts/pre-check.sh`：

```bash
#!/usr/bin/env bash
# Cadence pre-check 主脚本：六个基础工具的探测/安装/复验。
# 职责边界：仅处理 npx/uvx/ast-grep/codegraph/openspec/pi-mcp-adapter。
# 不处理 Superpowers 软链、OpenSpec 三客户端产物、Playwright、API Key（由 SKILL.md 处理）。
# 用法:
#   pre-check.sh run   [--mirror <name>] [--no-interrupt] [--upgrade]
#   pre-check.sh check [--mirror <name>] [--no-interrupt]
# 输出: stdout = 单份 JSON 报告；stderr = 彩色人类摘要。
# 兼容: mac bash 3.2 + BSD 工具 / Linux GNU 工具（POSIX 子集，无关联数组/grep -P）。

set -u
# 不用 set -e：需逐项捕获失败并汇总进 JSON，而非中途退出。

# 脚本所在目录（用于定位 mirrors/）
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MIRRORS_DIR="$SCRIPT_DIR/mirrors"

# 默认值
MODE=""
MIRROR="default"
NO_INTERRUPT=0
UPGRADE=0

# 颜色（stderr 摘要）
if [ -t 2 ]; then
  C_RED='\033[0;31m'; C_GRN='\033[0;32m'; C_YEL='\033[1;33m'; C_BLU='\033[0;34m'; C_NC='\033[0m'
else
  C_RED=''; C_GRN=''; C_YEL=''; C_BLU=''; C_NC=''
fi

log()  { printf '%b\n' "$*" >&2; }
err()  { printf '%b\n' "${C_RED}$*${C_NC}" >&2; }

usage() {
  cat >&2 <<'USAGE'
用法:
  pre-check.sh run   [--mirror <name>] [--no-interrupt] [--upgrade]
  pre-check.sh check [--mirror <name>] [--no-interrupt]
  pre-check.sh --help
说明:
  run            探测并对缺失工具执行安装与复验
  check          仅探测就绪状态，不安装
  --mirror name  加载 scripts/mirrors/<name>.sh（默认 default）
  --no-interrupt 任一基础工具失败即非零退出（失败关闭）
  --upgrade      查询当前源 latest 并升级落后工具（npm 系 + uv 本体）
USAGE
}

# 加载镜像配置；未知 mirror 报错并非零退出
load_mirror() {
  _name="$1"
  _file="$MIRRORS_DIR/$_name.sh"
  if [ ! -f "$_file" ]; then
    err "❌ 未知 mirror: $_name（未找到 $_file）"
    exit 2
  fi
  # shellcheck disable=SC1090
  . "$_file"
  : "${CADENCE_NPM_REGISTRY:?mirror 缺少 CADENCE_NPM_REGISTRY}"
  : "${CADENCE_PY_INDEX:?mirror 缺少 CADENCE_PY_INDEX}"
  : "${CADENCE_SUPERPOWERS_GIT:?mirror 缺少 CADENCE_SUPERPOWERS_GIT}"
}

# 命令级源注入助手
npm_registry_args() { printf '%s' "--registry=$CADENCE_NPM_REGISTRY"; }
uv_index_env()      { printf '%s' "UV_INDEX_URL=$CADENCE_PY_INDEX"; }

# 解析参数
while [ $# -gt 0 ]; do
  case "$1" in
    run|check)
      if [ -n "$MODE" ]; then err "❌ 子命令重复：$1"; usage; exit 2; fi
      MODE="$1"; shift ;;
    --mirror)
      [ $# -ge 2 ] || { err "❌ --mirror 缺少参数"; exit 2; }
      MIRROR="$2"; shift 2 ;;
    --mirror=*)
      MIRROR="${1#--mirror=}"; shift ;;
    --no-interrupt)
      NO_INTERRUPT=1; shift ;;
    --upgrade)
      UPGRADE=1; shift ;;
    --help|-h)
      usage; exit 0 ;;
    *)
      err "❌ 未知参数: $1"; usage; exit 2 ;;
  esac
done

if [ -z "$MODE" ]; then
  err "❌ 缺少子命令 run 或 check"
  usage
  exit 2
fi

# check 模式不支持 --upgrade（无安装动作，升级无意义）
if [ "$MODE" = "check" ] && [ "$UPGRADE" = "1" ]; then
  err "❌ check 模式不支持 --upgrade"
  exit 2
fi

load_mirror "$MIRROR"

# JSON 步骤累积（每项一行紧凑 JSON，由 Task 5 汇总）
STEPS_JSON=""

log "${C_BLU}🔧 pre-check${C_NC} mode=$MODE mirror=$MIRROR no_interrupt=$NO_INTERRUPT upgrade=$UPGRADE"
log "${C_BLU}📡 npm registry:${C_NC} $CADENCE_NPM_REGISTRY"
log "${C_BLU}🐍 python index:${C_NC} $CADENCE_PY_INDEX"
```

- [ ] **Step 2: 验证参数解析与 mirror 加载**

```bash
cd cadence-init/skills/pre-check/scripts
chmod +x pre-check.sh
./pre-check.sh --help
echo "exit=$?"
```

Expected: 打印用法，`exit=0`

- [ ] **Step 3: 验证未知 mirror 失败关闭**

```bash
./pre-check.sh run --mirror nonexistent; echo "exit=$?"
```

Expected: 输出 `❌ 未知 mirror: nonexistent`，`exit=2`

- [ ] **Step 4: 验证 check + --upgrade 互斥**

```bash
./pre-check.sh check --upgrade; echo "exit=$?"
```

Expected: 输出 `❌ check 模式不支持 --upgrade`，`exit=2`

- [ ] **Step 5: 验证默认 mirror 注入（stderr 显示官方源）**

```bash
./pre-check.sh run 2>&1 1>/dev/null | grep -E "registry|index"
```

Expected: 显示 `registry.npmjs.org` 与 `pypi.org/simple`

（注：此时脚本尚无六工具逻辑，run 会在打印源信息后结束，属正常；六工具在 Task 3 接入。）

- [ ] **Step 6: Commit**

```bash
git add cadence-init/skills/pre-check/scripts/pre-check.sh
git commit -m "feat(cadence-init): pre-check 主脚本框架（参数/mirror/源注入）"
```

### Task 3: 六工具探测、安装、复验与秒跳过（工作包 3）

**Files:**
- Modify: `cadence-init/skills/pre-check/scripts/pre-check.sh`（在 Task 2 框架末尾追加）

**Interfaces:**
- Consumes: Task 2 的 `MODE`、`NO_INTERRUPT`、`npm_registry_args`、`uv_index_env`、`STEPS_JSON`、`log/err`
- Produces: 函数 `add_step <name> <status> <action> <version> <error>`（把一项追加进 `STEPS_JSON`）、`probe_version <cmd...>`（输出版本号或返回非零）、`handle_failure <name> <msg>`（按 NO_INTERRUPT 决定终止或标记）；每工具一个 `do_<tool>` 函数；`FAILED_COUNT` 计数。供 Task 4（升级）与 Task 5（报告）使用。

- [ ] **Step 1: 追加通用辅助函数（版本探测、JSON 追加、失败处理）**

在 `pre-check.sh` 末尾（`load_mirror "$MIRROR"` 与日志行之后）追加：

```bash
# --- 通用辅助 ---

FAILED_COUNT=0

# 执行命令并输出首行版本号；失败返回非零，不打印到 stdout/stderr（避免污染 JSON）
probe_version() {
  _out="$("$@" 2>/dev/null | head -n 1 | tr -d '\r')" || return 1
  [ -n "$_out" ] || return 1
  printf '%s' "$_out"
}

# JSON 字符串转义（最小集：反斜杠、双引号、控制字符）
json_escape() {
  printf '%s' "$1" | sed -e 's/\\/\\\\/g' -e 's/"/\\"/g' -e ':a' -e 'N' -e '$!ba' -e 's/\n/\\n/g' -e 's/\t/\\t/g'
}

# 追加一个步骤到 STEPS_JSON（name/status/action/version/error）
add_step() {
  _n="$(json_escape "$1")"; _s="$(json_escape "$2")"; _a="$(json_escape "$3")"
  _v="$(json_escape "$4")"; _e="$(json_escape "$5")"
  _item="{\"name\":\"$_n\",\"status\":\"$_s\",\"action\":\"$_a\",\"version\":\"$_v\",\"error\":\"$_e\"}"
  if [ -z "$STEPS_JSON" ]; then STEPS_JSON="$_item"; else STEPS_JSON="$STEPS_JSON,$_item"; fi
}

# 失败处理：no-interrupt 立即非零退出；否则计数并继续
handle_failure() {
  _name="$1"; _msg="$2"
  FAILED_COUNT=$((FAILED_COUNT + 1))
  err "❌ $_name 失败：$_msg"
  if [ "$NO_INTERRUPT" = "1" ]; then
    err "🛑 no-interrupt 模式：立即终止"
    # 输出当前已累积 JSON 后退出（overall 由 Task 5 标记为 failed）
    emit_report "failed"
    exit 1
  fi
}
```

- [ ] **Step 2: 追加六工具处理函数（探测→跳过/安装→复验）**

继续追加：

```bash
# --- 六工具处理 ---
# 每个 do_<tool>：探测版本→已装则 ready 秒跳过；未装则 run 模式安装并复验，check 模式标记 failed。
# 安装命令输出全部重定向，避免污染 stdout 的 JSON。

INSTALL_TRIED=0   # 标记本次是否执行过安装（供摘要）

_try_install() {  # _try_install <描述> <安装命令...>
  _desc="$1"; shift
  if [ "$MODE" != "run" ]; then return 1; fi
  INSTALL_TRIED=1
  log "${C_YEL}⬇️  正在安装 $_desc ...${C_NC}"
  "$@" >/dev/null 2>&1
}

do_npx() {
  if _v="$(probe_version npx --version)"; then
    add_step "npx" "ready" "already-installed" "$_v" ""
    log "${C_GRN}✓ npx 已安装（$_v）${C_NC}"
  else
    # npx 随 Node.js/npm 提供，无法独立安装
    add_step "npx" "failed" "install-unavailable" "" "npx 未安装；需先安装 Node.js（脚本不自动安装 Node 运行时）"
    handle_failure "npx" "未检测到 npx，请先安装 Node.js"
  fi
}

do_uvx() {
  if _v="$(probe_version uvx --version)"; then
    add_step "uvx" "ready" "already-installed" "$_v" ""
    log "${C_GRN}✓ uvx 已安装（$_v）${C_NC}"
  else
    if [ "$MODE" = "run" ]; then
      # 经 pip 安装 uv（提供 uvx），走当前源
      if _try_install "uv（提供 uvx）" env "$(uv_index_env)" pip install uv; then :; fi
    fi
    if _v="$(probe_version uvx --version)"; then
      add_step "uvx" "installed" "installed-via-pip" "$_v" ""
      log "${C_GRN}✓ uvx 安装成功（$_v）${C_NC}"
    else
      add_step "uvx" "failed" "install-attempted" "" "uvx 安装失败或未就绪；可手动执行 pip install uv"
      handle_failure "uvx" "安装后复验失败"
    fi
  fi
}

do_ast_grep() {
  if _v="$(probe_version ast-grep --version)"; then
    add_step "ast-grep" "ready" "already-installed" "$_v" ""
    log "${C_GRN}✓ ast-grep 已安装（$_v）${C_NC}"
  else
    _try_install "ast-grep" npm i @ast-grep/cli -g "$(npm_registry_args)"
    if _v="$(probe_version ast-grep --version)"; then
      add_step "ast-grep" "installed" "installed-via-npm" "$_v" ""
      log "${C_GRN}✓ ast-grep 安装成功（$_v）${C_NC}"
    else
      add_step "ast-grep" "failed" "install-attempted" "" "ast-grep 安装失败；可手动执行 npm i @ast-grep/cli -g"
      handle_failure "ast-grep" "安装后复验失败"
    fi
  fi
}

do_codegraph() {
  if _v="$(probe_version codegraph version)"; then
    add_step "codegraph" "ready" "already-installed" "$_v" ""
    log "${C_GRN}✓ codegraph 已安装（$_v）${C_NC}"
  else
    _try_install "codegraph" npm i -g @colbymchenry/codegraph "$(npm_registry_args)"
    if _v="$(probe_version codegraph version)"; then
      add_step "codegraph" "installed" "installed-via-npm" "$_v" ""
      log "${C_GRN}✓ codegraph 安装成功（$_v）${C_NC}"
    else
      add_step "codegraph" "failed" "install-attempted" "" "codegraph 安装失败；可手动执行 npm i -g @colbymchenry/codegraph"
      handle_failure "codegraph" "安装后复验失败"
    fi
  fi
}

do_openspec() {
  if _v="$(probe_version openspec --version)"; then
    add_step "openspec" "ready" "already-installed" "$_v" ""
    log "${C_GRN}✓ openspec 已安装（$_v）${C_NC}"
  else
    _try_install "openspec" npm install -g @fission-ai/openspec@latest "$(npm_registry_args)"
    if _v="$(probe_version openspec --version)"; then
      add_step "openspec" "installed" "installed-via-npm" "$_v" ""
      log "${C_GRN}✓ openspec 安装成功（$_v）${C_NC}"
    else
      add_step "openspec" "failed" "install-attempted" "" "openspec 安装失败；可手动执行 npm install -g @fission-ai/openspec@latest"
      handle_failure "openspec" "安装后复验失败"
    fi
  fi
}

do_pi_mcp_adapter() {
  # 条件项：pi 不存在则跳过（不算失败）
  if ! command -v pi >/dev/null 2>&1; then
    add_step "pi-mcp-adapter" "skipped" "pi-not-found" "" "未检测到 pi 可执行文件，跳过"
    log "${C_BLU}ℹ️  未检测到 pi，跳过 pi-mcp-adapter${C_NC}"
    return 0
  fi
  if pi list 2>/dev/null | grep -q "pi-mcp-adapter" || [ -d "$HOME/.pi/agent/npm/node_modules/pi-mcp-adapter" ]; then
    add_step "pi-mcp-adapter" "ready" "already-installed" "" ""
    log "${C_GRN}✓ pi-mcp-adapter 已安装${C_NC}"
  else
    _try_install "pi-mcp-adapter" pi install npm:pi-mcp-adapter
    if pi list 2>/dev/null | grep -q "pi-mcp-adapter" || [ -d "$HOME/.pi/agent/npm/node_modules/pi-mcp-adapter" ]; then
      add_step "pi-mcp-adapter" "installed" "installed-via-pi" "" ""
      log "${C_GRN}✓ pi-mcp-adapter 安装成功${C_NC}"
    else
      add_step "pi-mcp-adapter" "failed" "install-attempted" "" "pi-mcp-adapter 安装失败；可手动执行 pi install npm:pi-mcp-adapter"
      handle_failure "pi-mcp-adapter" "安装后复验失败"
    fi
  fi
}
```

- [ ] **Step 3: 暂不接通主流程（待 Task 5 统一接入）**

本 Task 仅新增函数定义，不在脚本末尾调用 `do_*` 主流程——因为 `emit_report`（Task 5 定义）与升级钩子（Task 4）尚未就位，提前接通会导致运行报错。主流程调用统一在 Task 5 Step 2 接入。本步无文件改动，仅说明顺序约束。

- [ ] **Step 4: 语法检查（函数已定义、主流程未接通）**

```bash
cd cadence-init/skills/pre-check/scripts
bash -n pre-check.sh && echo "syntax ok"
# 确认六工具函数已定义
grep -cE "^do_npx\(\)|^do_uvx\(\)|^do_ast_grep\(\)|^do_codegraph\(\)|^do_openspec\(\)|^do_pi_mcp_adapter\(\)" pre-check.sh
```

Expected: 输出 `syntax ok`，函数计数为 `6`

- [ ] **Step 5: 验证函数逻辑（临时 source 调用单个函数，不接通主流程）**

```bash
# 在子 shell 中 source 脚本（此时脚本末尾无主流程调用，source 不会触发执行），单独调用 do_ast_grep
( . ./pre-check.sh run 2>/dev/null; do_ast_grep ) 2>&1 1>/dev/null | grep -E "ast-grep"
```

Expected: 输出 `✓ ast-grep 已安装（...）`（已装环境，走 ready 秒跳过，不触发安装）。注：source 时参数解析会消耗 `run`，`load_mirror default` 生效；因主流程未接通，仅手动调用的 `do_ast_grep` 执行。

- [ ] **Step 6: Commit**

```bash
git add cadence-init/skills/pre-check/scripts/pre-check.sh
git commit -m "feat(cadence-init): pre-check 六工具探测/安装/复验与秒跳过"
```

### Task 4: 升级能力（--upgrade opt-in，以当前源为准）（工作包 4）

**Files:**
- Modify: `cadence-init/skills/pre-check/scripts/pre-check.sh`（追加升级函数定义；主流程与升级钩子由 Task 5 统一接入）

**Interfaces:**
- Consumes: Task 2 的 `UPGRADE`、`npm_registry_args`、`uv_index_env`；Task 3 的 `probe_version`、`add_step`、`log/err`
- Produces: 函数 `npm_latest <pkg>`（查当前源 latest）、`upgrade_npm_tool <name> <pkg> <probe-cmd...>`、`upgrade_uv`。升级仅在 `UPGRADE=1` 且工具已 ready 时触发，把对应 `steps[]` 项从 ready 改为 upgraded（含 from/to/source）。

- [ ] **Step 1: 追加升级辅助函数**

在 `do_pi_mcp_adapter` 之后、主流程之前插入：

```bash
# --- 升级（opt-in）---
# 仅在 UPGRADE=1 时调用。范围：ast-grep/codegraph/openspec（npm 系）+ uv 本体。
# 不含 pi-mcp-adapter、uvx 临时包、playwright-cli。以当前源 latest 为准，不跨源比对。

# 规范化版本号为可比较的三段数字（取首个形如 x.y.z 的子串）
_norm_ver() { printf '%s' "$1" | sed -n 's/.*\([0-9][0-9]*\.[0-9][0-9]*\.[0-9][0-9]*\).*/\1/p' | head -n1; }

# 版本比较：返回 0 表示 $1 < $2（落后需升级），否则非 0。依赖 sort -V（GNU/BSD 均有）。
_ver_lt() {
  _a="$(_norm_ver "$1")"; _b="$(_norm_ver "$2")"
  [ -n "$_a" ] && [ -n "$_b" ] || return 1
  [ "$_a" = "$_b" ] && return 1
  [ "$(printf '%s\n%s\n' "$_a" "$_b" | sort -V | head -n1)" = "$_a" ]
}

# 查询 npm 包当前源 latest 版本
npm_latest() {
  npm view "$1" version "$(npm_registry_args)" 2>/dev/null | head -n1 | tr -d '\r'
}

# 升级某个 npm 工具：<显示名> <包名> <探测命令...>
upgrade_npm_tool() {
  _name="$1"; _pkg="$2"; shift 2
  _cur="$(probe_version "$@")" || return 0   # 未安装则跳过升级（安装归 do_*）
  _lat="$(npm_latest "$_pkg")"
  [ -n "$_lat" ] || { log "${C_YEL}⚠️  $_name 无法查询 latest，跳过升级${C_NC}"; return 0; }
  if _ver_lt "$_cur" "$_lat"; then
    log "${C_YEL}⬆️  升级 $_name：$_cur → $_lat（来源 $CADENCE_NPM_REGISTRY）${C_NC}"
    npm install -g "$_pkg@latest" "$(npm_registry_args)" >/dev/null 2>&1
    _new="$(probe_version "$@")"
    add_step "$_name" "upgraded" "upgraded" "$_new" "from=$_cur to=$_lat source=$CADENCE_NPM_REGISTRY"
    return 2   # 返回 2 表示已升级（供调用方覆盖原 ready 项）
  fi
  return 0
}

# 升级 uv 本体
upgrade_uv() {
  _cur="$(probe_version uv --version)" || return 0
  _lat="$(env "$(uv_index_env)" pip index versions uv 2>/dev/null | sed -n 's/.*(\([0-9][^)]*\)).*/\1/p' | head -n1)"
  [ -n "$_lat" ] || { log "${C_YEL}⚠️  uv 无法查询 latest，跳过升级${C_NC}"; return 0; }
  if _ver_lt "$_cur" "$_lat"; then
    log "${C_YEL}⬆️  升级 uv：$_cur → $_lat（来源 $CADENCE_PY_INDEX）${C_NC}"
    env "$(uv_index_env)" pip install -U uv >/dev/null 2>&1
    _new="$(probe_version uv --version)"
    add_step "uv" "upgraded" "upgraded" "$_new" "from=$_cur to=$_lat source=$CADENCE_PY_INDEX"
    return 2
  fi
  return 0
}
```

- [ ] **Step 2: 说明顺序约束（升级钩子由 Task 5 接入）**

本 Task 仅新增升级函数定义，主流程与升级钩子统一在 Task 5 Step 2 接入（`emit_report` 就位后）。本步无额外改动，仅说明顺序约束。

- [ ] **Step 3: 语法检查与函数存在性**

```bash
cd cadence-init/skills/pre-check/scripts
bash -n pre-check.sh && echo "syntax ok"
grep -cE "^upgrade_npm_tool\(\)|^upgrade_uv\(\)|^npm_latest\(\)|^_ver_lt\(\)" pre-check.sh
```

Expected: 输出 `syntax ok`，函数计数为 `4`

- [ ] **Step 4: 验证版本比较函数 _ver_lt**

```bash
( . ./pre-check.sh run 2>/dev/null
  _ver_lt "0.7.0" "0.7.3" && echo "0.7.0 < 0.7.3 → 需升级 (ok)"
  _ver_lt "0.7.3" "0.7.3" || echo "0.7.3 = 0.7.3 → 不升级 (ok)"
  _ver_lt "0.8.0" "0.7.3" || echo "0.8.0 > 0.7.3 → 不升级 (ok)"
)
```

Expected: 三行均以 `(ok)` 结尾

- [ ] **Step 5: 验证升级范围不 pi-mcp-adapter/playwright/uvx 包**

```bash
# 确认升级函数未被这些工具调用（主流程将在 Task 5 接入，此处静态检查）
grep -nE "upgrade_npm_tool \"pi-mcp-adapter\"|upgrade.*playwright|upgrade.*minimax|upgrade.*mcp-server-time" pre-check.sh && echo "⚠️ 发现越界升级" || echo "升级范围正确（仅 npm 系 + uv）"
```

Expected: 输出 `升级范围正确（仅 npm 系 + uv）`

- [ ] **Step 6: Commit**

```bash
git add cadence-init/skills/pre-check/scripts/pre-check.sh
git commit -m "feat(cadence-init): pre-check --upgrade 升级能力（npm 系 + uv，镜像即权威）"
```

### Task 5: JSON 报告（stdout）与人类摘要（stderr）与 next_actions（工作包 5）

**Files:**
- Modify: `cadence-init/skills/pre-check/scripts/pre-check.sh`（追加 `emit_report`，并在主流程末尾调用）

**Interfaces:**
- Consumes: Task 3 的 `STEPS_JSON`、`FAILED_COUNT`、`INSTALL_TRIED`；Task 2 的 `MODE`、`MIRROR`、`NO_INTERRUPT`、`UPGRADE`、`CADENCE_SUPERPOWERS_GIT`
- Produces: 函数 `emit_report <overall>`——向 stdout 输出单份完整 JSON；主流程末尾据此计算 overall 并以对应退出码结束。JSON 含 `mirror/mode/no_interrupt/overall/steps[]/next_actions/hints.superpowers_git`。

- [ ] **Step 1: 追加 emit_report 函数**

在 `json_escape` 等辅助之后、主流程之前插入：

```bash
# --- 报告 ---

# 计算整体状态：有失败→failed（no-interrupt）或 partial；否则 success
compute_overall() {
  if [ "$FAILED_COUNT" -gt 0 ]; then
    if [ "$NO_INTERRUPT" = "1" ]; then printf 'failed'; else printf 'partial'; fi
  else
    printf 'success'
  fi
}

# 输出单份 JSON 到 stdout。<overall> 可由 handle_failure 强制传 failed。
emit_report() {
  _overall="${1:-$(compute_overall)}"
  _ts="$(date -u +"%Y-%m-%dT%H:%M:%S.000Z")"
  _git="$(json_escape "$CADENCE_SUPERPOWERS_GIT")"
  _mirror="$(json_escape "$MIRROR")"
  _mode="$(json_escape "$MODE")"
  printf '{\n'
  printf '  "mirror": "%s",\n' "$_mirror"
  printf '  "mode": "%s",\n' "$_mode"
  printf '  "no_interrupt": %s,\n' "$NO_INTERRUPT"
  printf '  "upgrade": %s,\n' "$UPGRADE"
  printf '  "finished_at": "%s",\n' "$_ts"
  printf '  "overall": "%s",\n' "$_overall"
  printf '  "steps": [%s],\n' "$STEPS_JSON"
  printf '  "next_actions": ["superpowers-sync","openspec-clients","playwright-optional","apikey-placeholder"],\n'
  printf '  "hints": {"superpowers_git": "%s"}\n' "$_git"
  printf '}\n'
}
```

- [ ] **Step 2: 在主流程末尾输出报告并设定退出码**

在脚本末尾写入完整主流程段（Task 3/4 仅定义了函数，此处首次接通：六工具调用 → 升级钩子 → 汇总报告 → 退出码）：

```bash
# --- 主流程 ---
do_npx
do_uvx
do_ast_grep
do_codegraph
do_openspec
do_pi_mcp_adapter

# 升级钩子：仅 UPGRADE=1 时执行；仅升级已 ready 的工具
if [ "$UPGRADE" = "1" ]; then
  log "${C_BLU}⬆️  升级模式（来源：当前 mirror）${C_NC}"
  upgrade_npm_tool "ast-grep" "@ast-grep/cli" ast-grep --version
  upgrade_npm_tool "codegraph" "@colbymchenry/codegraph" codegraph version
  upgrade_npm_tool "openspec" "@fission-ai/openspec" openspec --version
  upgrade_uv
fi

# 汇总输出
_OVERALL="$(compute_overall)"
emit_report "$_OVERALL"

if [ "$_OVERALL" = "failed" ]; then
  exit 1
elif [ "$_OVERALL" = "partial" ]; then
  exit 0   # 普通模式部分失败仍以 0 结束，由 SKILL.md 读 overall 判定
else
  exit 0
fi
```

- [ ] **Step 3: 验证 stdout 是单份可解析 JSON**

```bash
cd cadence-init/skills/pre-check/scripts
./pre-check.sh check 2>/dev/null | python3 -m json.tool >/dev/null && echo "valid json"
```

Expected: 输出 `valid json`（stdout 可被 `python3 -m json.tool` 解析）

- [ ] **Step 4: 验证 stdout 不混入安装日志**

```bash
./pre-check.sh run 2>/dev/null | grep -cE "npm |added [0-9]+ packages|Downloading|Installing"
```

Expected: 输出 `0`（stdout 仅 JSON，无 npm/uv 安装日志）

- [ ] **Step 5: 验证 JSON 关键字段与枚举**

```bash
./pre-check.sh check 2>/dev/null | python3 -c "
import sys, json
d = json.load(sys.stdin)
assert d['overall'] in ('success','partial','failed'), d['overall']
assert isinstance(d['steps'], list) and len(d['steps']) >= 5, len(d['steps'])
allowed = {'ready','installed','upgraded','skipped','failed'}
for s in d['steps']:
    assert s['status'] in allowed, s
    for k in ('name','status','action','version','error'):
        assert k in s, (k, s)
assert d['next_actions'] == ['superpowers-sync','openspec-clients','playwright-optional','apikey-placeholder']
assert 'superpowers_git' in d['hints']
print('json schema ok; overall =', d['overall'])
"
```

Expected: 输出 `json schema ok; overall = success`（全已装环境）或对应状态

- [ ] **Step 6: 验证 hints.superpowers_git 随 mirror 切换**

```bash
echo "default:"; ./pre-check.sh check 2>/dev/null | python3 -c "import sys,json;print(json.load(sys.stdin)['hints']['superpowers_git'])"
echo "cn:";      ./pre-check.sh check --mirror cn 2>/dev/null | python3 -c "import sys,json;print(json.load(sys.stdin)['hints']['superpowers_git'])"
```

Expected:
- default 行输出 `https://github.com/obra/superpowers`
- cn 行输出 `https://gitee.com/michaelChe-World/superpowers.git`

- [ ] **Step 7: Commit**

```bash
git add cadence-init/skills/pre-check/scripts/pre-check.sh
git commit -m "feat(cadence-init): pre-check JSON 报告与人类摘要、next_actions"
```

### Task 6: 失败语义与端到端验证（no-interrupt / 平台 / 镜像）（工作包 6、7）

**Files:**
- Modify: `cadence-init/skills/pre-check/scripts/pre-check.sh`（如需微调失败路径）
- Test: 端到端命令验证（不新增测试文件，沿用仓库脚本验证惯例）

**Interfaces:**
- Consumes: Task 1-5 全部产物
- Produces: 经验证的脚本行为基线——no-interrupt 失败关闭、check 不安装、镜像切换、JSON 可解析、秒跳过。供 SKILL.md（Task 7）以退出码 + overall 双重判定。

- [ ] **Step 1: 验证 no-interrupt 失败关闭（模拟 npx 缺失场景）**

```bash
cd cadence-init/skills/pre-check/scripts
# 用受限 PATH 模拟 npx 缺失（仅保留系统基础命令）
env PATH="/usr/bin:/bin" ./pre-check.sh run --no-interrupt 2>/dev/null
echo "exit=$?"
```

Expected:
- `exit=1`
- stdout JSON 的 `overall` 为 `failed`
- stderr 出现 `no-interrupt 模式：立即终止`

- [ ] **Step 2: 验证 no-interrupt 下 npx 缺失立即终止，不继续后续安装**

```bash
env PATH="/usr/bin:/bin" ./pre-check.sh run --no-interrupt 2>&1 1>/dev/null | grep -c "正在安装"
```

Expected: 输出 `0`（npx 失败即终止，未进入后续工具的安装）

- [ ] **Step 3: 验证普通模式失败标记 partial 且继续**

```bash
# 用一个不存在的工具路径触发单项失败（构造 PATH 使 npx 缺失但允许其他）
env PATH="/usr/bin:/bin" ./pre-check.sh check 2>/dev/null | python3 -c "import sys,json;d=json.load(sys.stdin);print('overall=',d['overall']);print('npx=',[s['status'] for s in d['steps'] if s['name']=='npx'])"
echo "exit=$?"
```

Expected:
- `overall= partial`（或 failed，取决于缺失项）
- npx 的 status 为 `failed`
- check 模式不安装，`exit=0`

- [ ] **Step 4: 验证 check 模式全程不安装**

```bash
./pre-check.sh check 2>&1 1>/dev/null | grep -c "正在安装"
```

Expected: 输出 `0`

- [ ] **Step 5: 验证 mac/Linux 兼容性（POSIX 子集）**

```bash
# 语法检查 + shellcheck（若可用）+ 确认无 GNU 专属特性
bash -n ./pre-check.sh && echo "bash -n ok"
command -v shellcheck >/dev/null 2>&1 && shellcheck -s bash ./pre-check.sh || echo "shellcheck 未安装，跳过"
# 确认未使用关联数组与 grep -P
grep -nE "declare -A|grep -P|sed -i" ./pre-check.sh && echo "⚠️ 发现平台分歧特性" || echo "无 GNU/BSD 分歧特性"
```

Expected:
- `bash -n ok`
- `无 GNU/BSD 分歧特性`（无 `declare -A`、`grep -P`、`sed -i`）

- [ ] **Step 6: 在 mac（若可用）执行关键路径验证**

```bash
# 仅在有 mac 环境时执行；无则在 Linux 用 sh/bash3 兼容模式粗验
./pre-check.sh check 2>/dev/null | python3 -m json.tool >/dev/null && echo "mac/linux json ok"
```

Expected: 输出 `mac/linux json ok`

- [ ] **Step 7: 验证 cn 镜像源注入到安装命令（dry 观察）**

```bash
# 通过 --mirror cn 触发，观察 stderr 显示的源
./pre-check.sh check --mirror cn 2>&1 1>/dev/null | grep -E "registry|index"
```

Expected: 显示 `registry.npmmirror.com` 与 `pypi.tuna.tsinghua.edu.cn`

- [ ] **Step 8: 验证未写用户全局配置**

```bash
# 确认脚本不含写全局配置的命令
grep -nE "npm config set|git config --global|\.npmrc|uv/uv\.toml" ./pre-check.sh && echo "⚠️ 发现全局配置写入" || echo "无全局配置写入"
```

Expected: 输出 `无全局配置写入`

- [ ] **Step 9: Commit**

```bash
git add cadence-init/skills/pre-check/scripts/pre-check.sh
git commit -m "test(cadence-init): pre-check 失败语义与端到端验证（no-interrupt/平台/镜像）"
```

### Task 7: SKILL.md 改写——六工具移交脚本、Superpowers 读镜像地址（工作包 8）

**Files:**
- Modify: `cadence-init/skills/pre-check/SKILL.md`

**Interfaces:**
- Consumes: Task 6 验证后的脚本（`run`/`check`、`--mirror`、`--no-interrupt`、`--upgrade`、JSON 报告）；Task 1 的 `$CADENCE_SUPERPOWERS_GIT`
- Produces: 改写后的 SKILL.md——六工具改为脚本调用约定；Superpowers 步骤读 `$CADENCE_SUPERPOWERS_GIT`；保留 Superpowers 软链/OpenSpec 三客户端/Playwright/API Key 四项

**改写总原则**：六工具（npx/uvx/ast-grep/codegraph/openspec CLI/pi-mcp-adapter）的逐条安装与验证命令全部删除，替换为单一脚本调用；OpenSpec 三客户端产物判断（步骤 5 后半）、Superpowers 软链（步骤 6）、Playwright（可选步骤）、API Key（默认步骤）保留。

- [ ] **Step 1: 失败验证（确认现状含逐条安装命令）**

```bash
cd cadence-init/skills/pre-check
grep -c "npm i @ast-grep/cli -g\|npm i -g @colbymchenry/codegraph\|npm install -g @fission-ai/openspec\|pi install npm:pi-mcp-adapter" SKILL.md
```

Expected: 输出 ≥ 3（现状含逐条安装命令，改写后应只剩脚本调用）

- [ ] **Step 2: 在 `## 实施步骤` 开头插入脚本调用约定段**

在 `## 实施步骤` 标题之后、`### 步骤 1：检查 npx` 之前，插入以下新段落（作为步骤 0）：

````markdown
### 步骤 0：执行脚本完成六个基础工具检查

六个基础工具（npx、uvx、ast-grep、codegraph、openspec CLI、pi-mcp-adapter）的就绪探测、缺失安装与安装后复验统一由脚本完成，不再逐条执行安装命令。

**脚本位置**：`scripts/pre-check.sh`（相对本 SKILL.md 所在目录）

**调用命令**：

```bash
# 通用源，普通模式
bash scripts/pre-check.sh run

# 大陆镜像源
bash scripts/pre-check.sh run --mirror cn

# no-interrupt 模式（任一基础工具失败即非零退出）
bash scripts/pre-check.sh run --mirror cn --no-interrupt

# 仅探测不安装（摸底）
bash scripts/pre-check.sh check --mirror cn

# 升级已装工具到当前源 latest（npm 系 + uv 本体）
bash scripts/pre-check.sh run --mirror cn --upgrade
```

**读取结果**：脚本向 stdout 输出单份 JSON，向 stderr 输出彩色摘要。用以下命令取 overall 与各工具状态：

```bash
bash scripts/pre-check.sh run --mirror cn 2>/dev/null > /tmp/precheck.json
# overall（success/partial/failed）
python3 -c "import json;print(json.load(open('/tmp/precheck.json'))['overall'])"
# 某工具状态
python3 -c "import json;d=json.load(open('/tmp/precheck.json'));print([s for s in d['steps'] if s['name']=='ast-grep'])"
# Superpowers 远端地址（供步骤 6 使用）
python3 -c "import json;print(json.load(open('/tmp/precheck.json'))['hints']['superpowers_git'])"
```

**JSON 结构**（权威）：`overall`（success/partial/failed）、`steps[]`（每项 `name`/`status`/`action`/`version`/`error`，status 枚举 ready/installed/upgraded/skipped/failed）、`next_actions`、`hints.superpowers_git`。

**判定规则**：
- `overall=success` 且六工具 status 均为 ready/installed/upgraded/skipped：基础工具门槛通过，继续步骤 5 的 OpenSpec 三客户端检查与步骤 6 的 Superpowers 同步。
- 脚本非零退出或 `overall=failed`：no-interrupt 模式立即终止 `/pre-check`，报告失败；普通模式报告失败项与恢复建议，不宣称成功。
- pi-mcp-adapter 的 `status=skipped`（action=pi-not-found）不算失败。
````

- [ ] **Step 3: 删除六工具的逐条实施步骤**

删除以下整段（六工具的逐条安装/验证命令，已由脚本承接）：
- `### 步骤 1：检查 npx`（第 232 行起）整节
- `### 步骤 2：检查 uvx` 整节
- `### 步骤 3：检查 ast-grep` 整节
- `### 步骤 4：检查 codegraph` 整节
- `### 步骤 7：检查 pi MCP Adapter（条件检查）` 整节（脚本已承接其条件检查与安装）

**保留**：`### 可选步骤：检查 playwright-cli`（可选按需项，不进脚本）；`### 步骤 5：检查 OpenSpec` 中"初始化与更新命令""增量要求""验证命令"的**三客户端产物判断**部分（删除其中 openspec CLI 的 `npm install` 安装命令行，改为引用步骤 0 脚本结果）。

- [ ] **Step 4: 修改步骤 5 的 OpenSpec CLI 安装引用**

在 `### 步骤 5：检查 OpenSpec` 中，将 openspec CLI 的安装命令块：

```
old: **安装命令**：
     ```bash
     npm install -g @fission-ai/openspec@latest
     ```
new: **安装**：openspec CLI 由步骤 0 脚本统一安装与验证（见 `steps[]` 中 `name=openspec` 项）；CLI 未就绪时先回到步骤 0 处理，再继续本节三客户端产物检查。
```

- [ ] **Step 5: 修改步骤 6 Superpowers 的 clone/pull 地址读镜像变量**

在 `### 步骤 6：检查 Superpowers` 的"在线安装来源"处：

```
old: ```bash
     git clone https://github.com/obra/superpowers "$HOME/.agents/superpowers"
     ```
new: ```bash
     # 远端地址由步骤 0 脚本报告的 hints.superpowers_git 提供：
     # 通用源为 https://github.com/obra/superpowers；cn 镜像为用户维护的国内地址。
     git clone "$CADENCE_SUPERPOWERS_GIT" "$HOME/.agents/superpowers"
     ```
     `$CADENCE_SUPERPOWERS_GIT` 从步骤 0 的 JSON 报告 `hints.superpowers_git` 读取；使用 cn 镜像时直接 clone 国内地址，不配置 git 代理、不修改 git 全局配置。
```

- [ ] **Step 6: 更新 `## 快速参考` 表**

将快速参考表中六个工具的"检查命令或路径"列改为指向脚本，删除逐条命令：

```
old: | **1. npx** | `npx --version` | 输出版本号 | 自动安装稳定版本 |
     | **2. uvx** | `uvx --version` | 输出版本号 | 自动安装稳定版本 |
     | **3. ast-grep** | `ast-grep --version` | 输出版本号 | 自动全局安装 `@ast-grep/cli` |
     | **4. codegraph** | `codegraph version` | 输出版本号 | 自动全局安装 `@colbymchenry/codegraph` |
new: | **1-6. 基础工具** | `bash scripts/pre-check.sh run [--mirror cn]` | JSON `overall=success` 且六工具 status 就绪 | 脚本统一安装/复验；失败按模式处理 |
     | （含 npx/uvx/ast-grep/codegraph/openspec/pi-mcp-adapter） | `--upgrade` 升级 npm 系 + uv | `steps[].status` ∈ ready/installed/upgraded/skipped | 见步骤 0 |
```

- [ ] **Step 7: 更新 `### no-interrupt 强制完成策略` 表**

将表中六个基础检查行的判定改为脚本驱动：

```
old: | npx | `npx --version` 成功 | 立即终止 |
     ...（uvx/ast-grep/codegraph 各行）
new: | 六个基础工具 | 脚本 `run --no-interrupt` 退出码为 0 且 JSON `overall=success` | 立即终止 |
```

（OpenSpec 三客户端、Superpowers、Playwright、API Key 各行保留原语义。）

- [ ] **Step 8: 更新 `## 增量运行` 说明**

在增量原则处补充脚本语义：

```
在"增量原则"列表后追加一行：
- 脚本化执行：六个基础工具由 `scripts/pre-check.sh` 毫秒级本地版本探测，已就绪工具秒跳过、不查远端、不重装；仅缺失或携带 `--upgrade` 时才执行安装/升级。
```

- [ ] **Step 9: 验证 SKILL.md 不再含逐条安装命令**

```bash
cd cadence-init/skills/pre-check
grep -c "npm i @ast-grep/cli -g\|npm i -g @colbymchenry/codegraph\|npm install -g @fission-ai/openspec@latest\|pi install npm:pi-mcp-adapter" SKILL.md
```

Expected: 输出 `0`（逐条安装命令已全部移除，仅剩步骤 0 脚本调用）

- [ ] **Step 10: 验证 SKILL.md 含脚本调用与镜像地址读取**

```bash
grep -c "scripts/pre-check.sh\|hints.superpowers_git\|CADENCE_SUPERPOWERS_GIT" SKILL.md
```

Expected: 输出 ≥ 3（含脚本调用、报告字段、镜像变量）

- [ ] **Step 11: Commit**

```bash
git add cadence-init/skills/pre-check/SKILL.md
git commit -m "refactor(cadence-init): pre-check SKILL.md 六工具移交脚本、Superpowers 读镜像地址"
```

### Task 8: 一致性核验与契约同步（工作包 9）

**Files:**
- Modify: `openspec/changes/add-pre-check-shell-scripts/tasks.md`（勾选完成项）

**Interfaces:**
- Consumes: Task 1-7 全部产物、三个 spec 的验收场景
- Produces: 逐条核验记录；tasks.md checkbox 全部勾选

- [ ] **Step 1: 对照 pre-check-shell-execution spec 逐条核验**

```bash
cd cadence-init/skills/pre-check/scripts
# 单一主脚本承接六工具
grep -cE "do_npx|do_uvx|do_ast_grep|do_codegraph|do_openspec|do_pi_mcp_adapter" pre-check.sh
# run/check 子命令
grep -cE "run\|check" pre-check.sh
# 秒跳过（探测函数）
grep -c "probe_version" pre-check.sh
# 升级 opt-in
grep -c "UPGRADE" pre-check.sh
# JSON 走 stdout
grep -c "emit_report" pre-check.sh
```

Expected: 各项计数 ≥ 1，脚本覆盖 spec 六条 Requirement

- [ ] **Step 2: 对照 pre-check-mirror-sources spec 逐条核验**

```bash
# 三变量
grep -l "CADENCE_NPM_REGISTRY" mirrors/default.sh mirrors/cn.sh
# default/cn 源值
grep -h "registry.npmjs.org\|registry.npmmirror.com" mirrors/*.sh
# Superpowers Git 镜像化
grep -h "CADENCE_SUPERPOWERS_GIT" mirrors/*.sh
# 不写全局配置
grep -cE "npm config set|git config --global" pre-check.sh mirrors/*.sh
```

Expected: 两个 mirror 文件各含三变量；源值正确；无全局配置写入（计数 0）

- [ ] **Step 3: 对照 init-skill-sequencing spec 核验 SKILL.md**

```bash
cd ../
# 六工具由脚本执行
grep -c "scripts/pre-check.sh" SKILL.md
# 报告驱动
grep -c "overall\|next_actions" SKILL.md
# OpenSpec 三客户端仍由 SKILL.md 处理
grep -c "openspec init --tools\|三客户端" SKILL.md
```

Expected: 各项 ≥ 1；SKILL.md 保留三客户端判断

- [ ] **Step 4: 端到端冒烟（全新 → 全就绪）**

```bash
cd cadence-init/skills/pre-check/scripts
./pre-check.sh check --mirror default 2>/dev/null | python3 -m json.tool >/dev/null && echo "default ok"
./pre-check.sh check --mirror cn      2>/dev/null | python3 -m json.tool >/dev/null && echo "cn ok"
```

Expected: 两行 ok，两镜像 JSON 均可解析

- [ ] **Step 5: openspec validate**

```bash
cd /home/michaelche/workspace/github/Cadence-skills
openspec validate add-pre-check-shell-scripts
```

Expected: `Change 'add-pre-check-shell-scripts' is valid`

- [ ] **Step 6: 勾选 tasks.md 全部 checkbox**

将 `openspec/changes/add-pre-check-shell-scripts/tasks.md` 中所有 `- [ ]` 改为 `- [x]`。

- [ ] **Step 7: Commit**

```bash
git add openspec/changes/add-pre-check-shell-scripts/tasks.md
git commit -m "chore(openspec): 勾选 add-pre-check-shell-scripts 全部任务完成"
```

---

## Self-Review 记录

- **Spec 覆盖**：pre-check-shell-execution 6 条 Requirement → Task 2/3/4/5/6；pre-check-mirror-sources 5 条 → Task 1/2/4/7；init-skill-sequencing 2 条 → Task 7/8。无遗漏。
- **占位符**：无 TBD/TODO；脚本含完整可运行代码；SKILL.md 给出新增段全文与删除范围。
- **类型/名称一致性**：`probe_version`/`add_step`/`handle_failure`/`emit_report`/`compute_overall`/`load_mirror`/`npm_registry_args`/`uv_index_env`/`upgrade_npm_tool`/`upgrade_uv` 在 Task 2-6 间一致；`STEPS_JSON`/`FAILED_COUNT`/`INSTALL_TRIED` 一致；`overall`/`steps[].status` 枚举与 Global Constraints 一致。
