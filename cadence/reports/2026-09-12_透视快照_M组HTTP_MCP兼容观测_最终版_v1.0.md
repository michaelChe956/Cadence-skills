# 透视快照：M 组 HTTP MCP 兼容观测（最终版·手动测试结论）

> 生成：2026-09-12 · 状态：**手动测试模式**（自动化车道已回退——PTY 驱动与各家 TUI 交互适配投入产出比不划算，2026-09-12 用户裁决）

## 结论（手动实测 + 排查证据）

| 探针 | claude | codex | pi | kimi | omp |
|---|---|---|---|---|---|
| M1 web 搜索 | ✅ | **⚙ 调用失败** | ✅ | ✅ | ✅ |
| M2 网页阅读 | ✅ | **⚙ 调用失败** | ✅ | ✅ | ✅ |
| M3 仓库阅读 | ✅ | **⚙ 调用失败** | ✅ | ✅ | ✅ |

**唯一问题端：codex**——rmcp streamable-http 客户端 × bigmodel 网关不兼容（网关 initialize 响应后关流，codex 视作 `Transport channel closed`，重试 3 轮死）。其余四端客户端容忍此行为。

## 排查过程存档（供后续同类问题参考）

1. **端点/key/网络全部正常**：curl 全序列（initialize→session-id→initialized）三端点 200
2. **codex 配置解析正确**：`codex mcp get` 显示 `http_headers: Authorization=*****` 已注册
3. **版本排除**：codex 0.153.4 与 0.154.0 同挂；容器内重放安装 0.154.0 通过（网络路径差异）
4. **评分器修复**：`mcp_called` 断言曾有两处假绿（静默忽略 + 失败调用被当使用），已修复（`require_ok`），修复过程详见本快照 v1.0 初版
5. **zai-mcp-server 不覆盖此三者**：纯视觉 8 工具 vs 联网搜索/网页阅读/仓库阅读——功能零交集

## 手动测试方法（有改动时执行）

```bash
# 1. 端点健康快查
curl -sS -m 15 -o /dev/null -w '%{http_code}' -X POST \
  https://open.bigmodel.cn/api/mcp/web_search_prime/mcp \
  -H "Authorization: Bearer <key>" -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"probe","version":"1.0"}}}'

# 2. 各端交互式验证：打开 claude/codex/pi/kimi/omp，说「用 web-search-prime 搜索 GLM 最新动态」
# 3. codex 预期 ⚙（已知不兼容）；其余四端预期 ✅
```

## 基础设施留存

- `eval/probes/definitions.py` M1/M2/M3 定义保留（手动测试规范/探针 prompt 题库）
- `eval/scoring/assertor.py` `require_ok` 修复保留（真 bug 修复，影响所有 mcp_called 断言）
- 自动化宿主车道（eval/hostlane/）已回退——设计/计划/OpenSpec 存档不删（决策记录）

## 修订记录

- 2026-09-11 v1.0：容器车道四轮排查（占位 key 伪影→headless 挂载差异→评分器假绿→三态修正）
- 2026-09-11 v1.1：三态判定修正
- 2026-09-12 最终版：自动化车道回退，结论以手动实测为准；codex ⚙ 为唯一真实问题端
