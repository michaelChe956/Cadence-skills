# 进度报告:五端渐进式规则架构两 change 落地验收(v1.0)

- **日期**:2026-09-10
- **范围**:OpenSpec change `support-omp-client`(A)+ `rules-progressive-load-probes`(B),依据设计文档 `cadence/designs/2026-09-09_方案设计_五端渐进式规则架构_v1.0.md`(oracle 三轮审核终裁满意)
- **实施方式**:A 主会话逐 Task(同文件串行);B subagent-driven(B-T1/B-T2/B-T3/B-T4 各一子代理+任务间主会话审查,B-T5 真实夜测主会话)

## 一、Change A——omp 第五端框架支持 + 考场五端化

| 工作包 | 交付 | 验证 |
|---|---|---|
| A1 模板 frontmatter | 10 源模板(8 落地规则)分桶 frontmatter(常驻/条件/行为路由/媒体触发);快照契约测试 5 用例 | `ut-fm-*` 全绿 |
| A1 L0 v5 | 内核 v5(omp 客户端语义行)、v4 归档 `l0-history/`、`L0_OLD_VERSIONS` 含 v4、升级路径 v4→v5 | `ut-l0-v5-*`/`ut-kernel-v5` 全绿 |
| A1 eval 断言层同步 | assertions.py `L0_BEGIN`/`l0.v4→l0.v5`/`v3.upgraded` 文案/`:352` 硬编码 end/docstring/测试 fixture 全部升 v5,无旧名并行 | `test_stage1_assertions` 13 用例全绿 |
| A1 s11_omp_bridge | `_bridge_actions`(create/repair/remove/conflict-backup/用户文件保留+warning)/`_apply_bridge_action`(symlink 失败物化降级)/`step_s11_omp_bridge`(`.omp/AGENTS.md` 受管活引用+旧文件归档);STEP_ORDER/STEP_FUNCS/dry-run 预览接入 | `ut-s11-*` 11 用例全绿;harness `mk_converged_rules` 收敛态含桥 |
| A1 文档 | PRUNE_DIRS+SKILL.md find 块加 `.omp`(双向断言);merge-semantics §11.8 OR-01~07;SKILL.md v5/概览/warnings(`OMP_BRIDGE_MATERIALIZED`/`OMP_BRIDGE_USER_FILE_KEPT`);skill-clause-map OR 对账 | `sc-prune-dirs-contract` 等 PASS |
| A1 verify | 第⑥项 `omp_assets`(桥集合一致+受管活引用一致;not-generated 不算漂移) | `ut-s11-verify` 绿 |
| A2 omp 容器化 | container.py omp ELF 复制+auth 最小集(models.yml/config.yml);session.py `omp -p` | 容器冒烟:version rc=0、无头「OK」rc=0 |
| A3 五端夜测 | report_matrix AGENTS 五端;night.py degrade 记录+透视注记 | **omp×P1-P8 = 8/8 PASS**(26 分钟,全新容器);降级演练三断言过;omp 基线首轮入 baseline.json;README 五端化 |

rule-config 全量 297 用例+harness 103/0。

## 二、Change B——R 组规则加载验证与透视

| 工作包 | 交付 | 验证 |
|---|---|---|
| B1 评分接线 | assertor `text_contains`/`text_lacks`;`_score_probe_text` 消费 `probe["assertions"]`(P 组分支零改动) | 8 用例;假绿不可能测试(R1 无锚点→FAIL)先红后绿 |
| B1 四条确定性断言 | `rules.frontmatter`/`omp.symlinks`/`omp.agents-md`/`agents-md.budget`;night `_write_stage1_assertions` 以 `probe_id="stage1"` schema 1.0 落盘 | 11 用例;mock 正反例+落盘 PASS/FAIL/拷贝异常 |
| B3 Claude 常载审计 | claude 专属 hook 注入(保守合并)/stage1 后截断/`_audit_resident_rules`(允许集=入口+用户级 CLAUDE.md+language+README;非 session_start 装载不计) | 12 用例;**真实夜测 PASS(loaded=6, violations=[])** |
| B4 R 组探针 | R1/R2/R3(两步作答)+`R_GROUP`+`agents_only`;omp 专属 fixture(永不匹配 glob+agents:[scout]+正文独有 marker);`_audit_omp_rollout` | 22 用例;端过滤实证(四端跳过 R3 不落记录) |
| B4 rollout 审计 | `<domain-rules>` **块内**双 marker 0 命中(块外规则名=codex-rules-inline 经 `.omp/AGENTS.md` 活引用的合法投影,不计违规——2026-09-10 容器实测定位后精化,契约三处同步) | **真实容器 R3 审计 PASS** |
| B5 夜测+基线+透视 | R1/R2 五端 1/1、R3 omp 2/2、stage1 五端全绿;R 组基线入 baseline.json(不动 P 组) | 透视矩阵含 R 组与 stage1 行;基线对比无降级 |

eval 全量 156 用例。

## 三、R1-R5 验收对账(设计文档 §11)

| 验收 | 证据 |
|---|---|
| R1 渐进加载 | claude 常载审计 PASS(仅允许集);omp rollout 块内正文/受限条目 0 命中;R1/R2 探针五端全绿 |
| R2 单一事实源 | s11 软链桥=唯一物理源投影;verify `omp_assets` 断言一致;stage1 断言五端全绿 |
| R3 agent 限定 | fixture(`agents:[scout]`)索引块内 0 命中(确定性);workflow 规则索引可见+`rule://` 可达(R3 行为 2/2);**框架不携带任何 agent 限定**(2026-09-10 用户修正:子代理自行读取初始化规则,agents 字段仅测试 fixture 使用);scout 正向探针出范围(契约注明) |
| R4 漂移治理 | `.omp/AGENTS.md` 活引用(零副本,codegraph 再生成自动跟随);受管区块外逐字保留(既有断言延续);`codex.inline` 既有断言在场 |
| R5 透视 | `report_matrix.py` 五端×(P+R+stage1)矩阵+基线对比+降级注记;26+14 条运行记录留档 `eval/results/docker-night/` |

## 三·五、2026-09-10 追记:R3 撤销

用户裁决(三项事实:大部分业务项目不用 omp;agent 设置随时频繁调整;直接在 omp 侧调整不固化到框架):R3(agent 限定)的机器验收整体撤销——删除 scout 测试夹具、R3 探针、rollout 审计代码、agents_only 机制、R3 基线;`agents:` 定位为 omp 原生可选字段,由用户在项目规则中自行添加,经软链桥字节透传即生效,框架与 eval 均不预置、不测试。eval 套件 138 用例全绿,历史夜测记录保留归档。

## 四、残留与后续

- 未 git commit(产物自动提交开关=关闭;工作树含两 change 全部改动,待用户审查提交)
- 用户仓(cadence-aria/naruto)吸收:各自重跑 `/rule-config` 即升级(frontmatter 权威覆盖+s11 桥+L0 v5)
- Change 归档(openspec archive)待用户验收后执行
