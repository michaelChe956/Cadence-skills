# 配置变更

> 本文档始终强制提供，所有字段必须填写且不得留空。Manifest 的 `scope.configurations.status` 为 `全量` 或 `指定` 时，基线和目标必须是同一环境的外部不可变快照，目录必须可读；无配置变更时仍需提供两份快照及判断依据。状态为 `不适用` 时，变更状态必须为 `无变更`，快照字段填写 `不适用（具体原因）`，并跳过快照比较。
>
> 配置差异明细只允许记录配置键、变更类型、用途和固定脱敏值 `<redacted>`。不得填写真实配置值、密码、Token、AccessKey、Secret、密钥、私钥、完整连接串、内部域名/IP/URL、原始配置片段或敏感值哈希；发现这些内容时整个变更包在计算幂等标识前停止。

- Manifest 配置领域状态：全量 / 指定 / 不适用
- 配置领域不适用原因：
- 变更状态：有变更 / 无变更
- 环境（基线与目标必须相同）：
- 基线快照标识：
- 基线快照发布批次：
- 基线快照生成或获取时间：
- 基线快照来源类型：
- 基线快照外部目录：
- 基线快照指纹：
- 基线范围摘要：
- 基线纳入文件数量或文件清单摘要：
- 基线服务摘要：
- 基线文件规则摘要：
- 目标快照标识：
- 目标快照发布批次：
- 目标快照生成或获取时间：
- 目标快照来源类型：
- 目标快照外部目录：
- 目标快照指纹：
- 目标范围摘要：
- 目标纳入文件数量或文件清单摘要：
- 目标服务摘要：
- 目标文件规则摘要：
- 纳入文件范围：
- 涉及服务：
- 配置组：
- 已知差异：
- 无变更判断依据：

## 配置差异明细

| 配置键 | 变更类型 | 用途 | 脱敏值 |
|--------|----------|------|--------|
|  | 新增 / 删除 / 修改 / 移动 / 无变更 |  | `<redacted>` |
