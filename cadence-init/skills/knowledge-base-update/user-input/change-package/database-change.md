# 数据库变更

> 本文档始终强制提供，所有字段必须填写且不得留空。非适用字段填写 `不适用（具体原因）`；无数据模型变更时必须填写无变更判断依据。DDL 或迁移证据必须先脱敏，不得包含真实配置值、凭证、完整连接串、未脱敏内部地址、真实个人数据或敏感值哈希；发现这些内容时整个变更包在计算幂等标识前停止。

- 变更状态：有变更 / 无变更
- 数据库 / Schema：
- 逻辑表：
- 字段变化：
- 索引变化：
- 约束变化：
- DDL 或迁移路径：
- 上线状态：
- 兼容性：
- 回滚方式：
- 无变更判断依据：
