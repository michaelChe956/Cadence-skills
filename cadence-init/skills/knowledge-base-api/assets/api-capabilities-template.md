# {标识} {能力名称}

> **能力 ID**：{稳定ID}
> **分类**：{对外/对内}
> **能力类型**：{REST/RPC/消息/队列/文件/任务/批处理}
> **数据来源**：{用户资料或代码来源}
> **梳理日期**：{YYYY-MM-DD}
> **参数与报文**：{配套文件链接或不适用}

## 一、能力基础信息

| 项目 | 值 |
|------|----|
| 能力名称 | {能力名称} |
| API 名称或逻辑标识 | {API名称或逻辑标识} |
| 分类 | {对外/对内} |
| 能力类型 | {能力类型} |
| 协议与方法 | {HTTP方法/RPC协议/消息协议/触发方式} |
| 路径或逻辑地址 | {路径或脱敏逻辑标识} |
| 版本 | {版本} |
| 调用方或生产者 | {调用方或生产者} |
| 落地方或消费者 | {落地方或消费者} |
| 生命周期状态 | {已声明/已实现/已装配/已暴露/条件启用/已废弃/状态未知} |

## 二、业务需求描述

{业务语义；未提供时填写“未提供”。}

## 三、输入参数

{请求响应能力引用参数与报文文件；其他能力填写输入模型、消息模型、文件格式或“不适用”。}

## 四、输出参数

{请求响应能力引用参数与报文文件；其他能力填写输出模型、副作用或“不适用”。}

## 五、代码实现定位

### 5.1 用户清单与代码映射

| 来源 | 标识 | 结论 | 证据 |
|------|------|------|------|
| 用户对外能力清单 | {清单标识} | {对外分类或未登记} | {文件与行号} |
| 当前代码 | {入口符号} | {实现状态} | {文件与行号} |

### 5.2 实现清单

| 层级 | 符号 | 文件路径 | 状态 | 说明 |
|------|------|----------|------|------|
| 入口 | {类或配置} | {文件路径} | {状态} | {说明} |

## 六、调用链路

### 6.1 调用树

```text
{API 稳定 ID/入口}
└─ {SERVICE/MODULE 稳定 ID与实现}
   ├─ {TABLE 稳定 ID}
   │  └─ {字段 → Mapper/SQL}
   └─ {CONFIGURATION 稳定 ID}
      └─ {配置键 → 绑定与生效条件}
```

### 6.2 分支与触发条件

| 条件 | 路径 | 结果 | 证据 |
|------|------|------|------|
| {条件} | {调用路径} | {结果} | {文件与行号} |

### 6.3 逐层调用明细

| 层级 | 符号 | 职责 | 下游 | 证据 |
|------|------|------|------|------|
| {层级} | {类.方法或配置} | {职责} | {下游} | {文件与行号} |

## 七、数据模型与配置依赖

### 7.1 数据模型影响

| TABLE 稳定 ID | Schema/逻辑表 | 读写 | 涉及字段 | API 模型映射 | Mapper/DAO/SQL | 表字段证据状态 | 端到端映射状态 | 表文档链接 |
|---------------|---------------|------|----------|--------------|----------------|------------------|------------------|------------|
| {TABLE-ID} | {Schema与逻辑表} | {R/W} | {读字段/写字段} | {请求或响应字段→表字段} | {符号或SQL} | {DDL 已确认/迁移已确认/代码可推导/用户提供/来源冲突/待确认} | {已确认/来源冲突/待确认} | [`{TABLE-ID}`](../data-models/{路径}.md) |

> 只摘录直接影响当前能力的字段。`表字段证据状态` 仅描述字段定义证据；`端到端映射状态` 判断 `API 模型 → SERVICE/MODULE → Mapper/SQL → TABLE 字段` 是否完整。API 模型字段到表字段必须由转换代码、Entity、Mapper 或 SQL 逐跳证明，不得根据字段同名建立关系。DDL 已确认不能代替端到端映射已确认；缺少任一关键跳时端到端状态填写“待确认”。

### 7.2 配置依赖

| 配置组稳定 ID | 服务配置实体 | 配置键 | 直接影响 | 环境/Profile | 生效条件与绑定 | 证据状态 | 配置文档链接 |
|----------------|--------------|--------|----------|--------------|--------------|----------|--------------|
| {CONFIG-ID} | {SERVICE-ID} | {配置键} | {请求/响应/副作用/路由/鉴权/中间件/外部系统} | {环境/Profile或待确认} | {绑定位置与条件} | {已确认/待确认} | [`{SERVICE-ID}`](../configurations/{服务文档}.md) |

> 只记录直接影响当前能力行为的配置。敏感值统一写 `<redacted>`；只有配置键但缺少绑定或生效条件时，证据状态填写“待确认”。

## 八、中间件使用明细

### 8.1 缓存与队列

| 类型 | 名称或 Key 模式 | 读写方向 | 触发时机 | 证据 |
|------|-----------------|----------|----------|------|

### 8.2 消息

| Topic/Queue/Group | 方向 | 消息模型 | 重试与幂等 | 证据 |
|-------------------|------|----------|------------|------|

### 8.3 搜索与本地缓存

{有则填写；没有填写“不适用”或“未发现”。}

### 8.4 RPC 与下游 HTTP

| 服务 | 协议 | 版本或分组 | 触发条件 | 证据 |
|------|------|------------|----------|------|

### 8.5 文件与对象存储

| 协议或存储 | 逻辑位置 | 文件格式 | 触发方 | 接收方 | 证据 |
|------------|----------|----------|--------|--------|------|

### 8.6 定时任务与批处理

| 任务 | 触发方式 | 并发与锁 | 重试与补偿 | 证据 |
|------|----------|----------|------------|------|

## 九、数据源与副作用分析

- 主数据来源：{数据源}
- 实时查询或补充路径：{路径}
- 写入、副作用或异步结果：{副作用}
- 事务、一致性和失败处理：{结论}

## 十、关键证据引用

| 引用 | 文件或资料位置 |
|------|----------------|
| 用户对外能力清单 | {文件与行号} |
| 入口定义 | {文件与行号} |
| 入口实现 | {文件与行号} |
| 数据访问 | {TABLE 稳定 ID、表文档链接、DDL、Mapper或SQL位置} |
| 配置依赖 | {服务配置实体、配置键、配置文档链接、绑定与生效条件} |
| 中间件与外部调用 | {配置或代码位置} |

## 十一、请求、响应或载荷示例

{请求响应能力引用参数与报文文件；消息、文件和任务填写载荷示例；没有时填写“未提供”。}
