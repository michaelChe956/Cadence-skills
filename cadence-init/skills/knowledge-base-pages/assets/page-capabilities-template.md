# 页面能力

## 文档元数据

- 生成时间：
- Git 分支：
- 基线提交：
- 执行模式：
- 前端应用：
- 动态路由限制：

## 1. 前端应用与路由来源

## 2. 路由树

## 3. 页面能力清单

| PAGE ID | ROUTE ID | 路径 | 页面用途 | 组件 | 权限 | 状态 | API 主文件 | 证据 |
|---------|----------|------|----------|------|------|------|------------|------|

## 4. 页面与 API 映射

### 4.1 已匹配接口

| PAGE ID | API ID | 分类 | 方法与标准路径 | 调用位置 | 请求封装、Store 或 Hook | 接口主文件 | SERVICE/MODULE | 状态 | 可信度 | 证据 |
|---------|--------|------|----------------|----------|-------------------------|------------|----------------|------|--------|------|

填写规则：

- `API ID` 必须与 `cadence/knowledge-base/interfaces/README.md` 和接口主文件一致。
- `接口主文件` 必须使用 Markdown 相对链接，目标位于 `../interfaces/`；不能只写文件名或 API ID。
- `方法与标准路径` 必须包含 HTTP Method，以及合并 baseURL、代理、网关和动态参数模板后的标准路径。
- 页面经 Store、Hook、Composable、Thunk 或 Service 间接调用时，在“请求封装、Store 或 Hook”中写出完整前端调用链。
- 同一页面调用多个 API 时逐条记录，不能用逗号合并成一个无法追踪的单元格。

### 4.2 页面字段与数据模型影响

| PAGE/ROUTE ID | API ID | SERVICE/MODULE | TABLE 稳定 ID | 页面字段 | API 字段来源 | 表字段 | 读写 | Mapper/SQL 或后端映射 | 证据状态 | 表文档链接 |
|---------------|--------|----------------|---------------|----------|------------|--------|------|----------------------|----------|------------|
| {PAGE-ID} | {API-ID} | {SERVICE-ID} | {TABLE-ID} | {展示/筛选/提交字段} | {请求/响应模型与前端映射} | {字段} | {R/W} | {符号或SQL} | {已确认/待确认} | [`{TABLE-ID}`](../data-models/{路径}.md) |

页面字段必须沿 `PAGE → API 模型/请求代码 → SERVICE/MODULE → Mapper/SQL → TABLE 字段` 逐跳取证。页面、API 与表字段同名不能作为关系证据；缺少任一关键跳时标记“待确认”。

### 4.3 后端服务配置依赖

| PAGE/ROUTE ID | API ID | SERVICE/MODULE | 配置组稳定 ID | 服务配置实体 | 配置键 | 页面影响 | 环境/Profile | 生效条件与绑定 | 证据状态 | 配置文档链接 |
|---------------|--------|----------------|----------------|--------------|--------|----------|--------------|--------------|----------|--------------|
| {PAGE-ID} | {API-ID} | {SERVICE-ID} | {CONFIG-ID} | {SERVICE-ID} | {配置键} | {路由/权限/请求/展示/Feature Flag} | {环境/Profile或待确认} | {API响应/错误→前端绑定；后端配置绑定与条件} | {已确认/待确认} | [`{SERVICE-ID}`](../configurations/{服务文档}.md) |

后端服务配置必须沿 `PAGE/ROUTE → API → SERVICE/MODULE → CONFIGURATION` 逐跳取证。必须同时填写 API ID 与 SERVICE/MODULE；纯前端绑定不能替代任一节点。Feature Flag 或后端开关只有在能够证明经 API 响应、错误或能力状态直接控制页面行为时才记录。敏感值统一写 `<redacted>`；缺少任一关键跳、绑定或生效条件时写“待确认”。

### 4.4 前端直接配置

| PAGE/ROUTE ID | 前端应用或模块 | 配置键 | 页面影响 | 环境/构建模式 | 前端绑定与生效条件 | 证据状态 | 来源 |
|---------------|----------------|--------|----------|---------------|--------------------|----------|------|
| {PAGE-ID} | {APP/MODULE-ID} | {环境变量或前端配置键} | {路由/权限/请求/展示} | {环境/构建模式或待确认} | {读取位置与条件} | {已确认/待确认} | {前端配置与代码位置} |

本表仅记录前端代码直接读取的环境变量、构建时 Feature Flag 或静态菜单配置，不链接 `configurations/` 后端服务配置实体，也不能据此建立后端 SERVICE/MODULE 或 CONFIGURATION 关系。敏感值统一写 `<redacted>`。

### 4.5 未匹配接口候选

| PAGE ID | 候选 API ID | 已知方法与路径 | 调用位置 | 接口主文件 | 状态 | 可信度 | 待确认项 |
|---------|-------------|----------------|----------|------------|------|--------|----------|

未在接口索引中找到唯一匹配项时：

- 使用 `API-CANDIDATE-*` 作为临时追踪 ID。
- `接口主文件` 填写“未登记”，不生成不存在的 Markdown 链接。
- 记录已知 Method、Path、调用位置和候选后端入口。
- 将核实任务写入 `open-questions.md`，交由 `knowledge-base-api` 补录或校对。

## 5. 权限与导航

## 6. 状态管理与请求封装

至少记录页面到标准 REST API 的规范化过程：

```text
PAGE
→ Store / Hook / Composable / Service
→ 请求函数
→ baseURL / 代理 / BFF / 网关重写
→ HTTP Method + 标准 Path
→ API ID 与接口主文件
```

## 7. 动态路由和运行时限制

## 8. 孤立与不可达页面

## 9. 来源冲突

## 10. 待人工确认

## 11. 完整追溯关系

```text
ROUTE
→ PAGE
→ API
→ SERVICE/MODULE
├─ TABLE → 字段/Mapper/SQL → data-models/ 文档
└─ CONFIGURATION → 配置键/环境/生效条件 → configurations/ 文档
```

API 节点必须使用与接口知识库相同的稳定 ID，并链接到对应接口主文件。TABLE 与 CONFIGURATION 关系必须经过 API 和 SERVICE/MODULE 证据，不能由页面字段或业务名称直接推断。
