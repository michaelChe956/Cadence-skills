# {{任务名称}}任务上下文

## 1. 任务识别

- 原始请求：{{用户请求}}
- Schema：Schema 4.0
- 主画像：{{主画像}}
- 辅助画像：{{辅助画像，没有则写“无”}}
- Manifest Git 基线：{{baseline_commit}}
- 当前提交：{{current_commit}}
- 工作区状态：{{clean 或工作区修改摘要}}
- 数据模型来源状态：{{evidence.data_model_sources 摘要}}
- 配置授权快照：{{快照标识、环境、授权指纹校验状态}}
- 最近处理变更包：{{update.last_change_package，没有则写“无”}}
- 读取范围：{{工程、模块和领域}}
- 生成时间：{{YYYY-MM-DD HH:mm:ss 时区}}

## 2. 任务理解

{{目标、边界、非目标和明确假设}}

## 3. 核心实体

| 稳定 ID / 符号 | 类型 | 名称 | 位置 | 与任务关系 |
|-----------------|------|------|------|------------|
| {{ID}} | {{类型}} | {{名称}} | {{知识库、代码、数据模型或配置位置}} | {{关系}} |

## 4. 四类证据矩阵

| 结论 | KnowledgeBase | 当前代码 | DDL/数据模型证据 | 配置快照证据 | 状态 | 任务影响 |
|------|---------------|----------|------------------|--------------|------|----------|
| {{结论}} | {{稳定 ID 与文档位置}} | {{文件、符号、调用链或测试}} | {{字段文档、DDL、迁移、Mapper、SQL 或 Entity}} | {{服务配置文档、快照文件、配置键、绑定与生效条件}} | {{证据状态}} | {{影响}} |

状态只允许：`一致`、`KnowledgeBase 缺失`、`源码缺失`、`数据模型证据缺失`、`配置证据缺失`、`基线漂移`、`来源冲突`、`待确认`。

## 5. 关系与影响面

```text
{{PAGE → API → SERVICE/MODULE → TABLE / 数据源 / 配置组 → 配置键 → 绑定代码}}
```

- 表关系检查：{{直接关系；没有则写“无直接表关系，已停止数据模型方向”}}
- 配置关系检查：{{直接关系；没有则写“无直接配置关系，已停止配置方向”}}

## 6. 数据模型上下文

- 逻辑表与字段：{{TABLE 稳定 ID、字段约束和证据状态}}
- 数据访问：{{Mapper、SQL、Entity、数据源和路由}}
- DDL / 迁移：{{路径或“缺失，已使用字段文档/迁移/代码证据继续”}}
- 未确认结构：{{没有则写“无”}}

## 7. 配置上下文

- 服务 / 配置组：{{稳定 ID 与用途}}
- 当前快照：{{环境、快照标识、授权指纹校验状态和只读目录状态}}
- 配置键：{{键、用途、状态、绑定位置；敏感值写 `<redacted>`}}
- 生效条件：{{Profile、环境覆盖、条件装配、加载顺序}}
- 数据源 / 分片关系：{{关系或“无直接关系”}}

## 8. 相关变更包

| 变更包 | 处理状态 | MR / 提交 | 数据库文档 | 配置文档 | 与任务关系 | 基线影响 |
|--------|----------|-----------|------------|----------|------------|----------|
| {{CHANGE-*}} | {{已处理 / 未处理 / 无}} | {{位置}} | {{状态与位置}} | {{状态与位置}} | {{关系}} | {{无 / 基线漂移}} |

## 9. 画像专属上下文

{{按主画像和辅助画像填写对应专属输出}}

## 10. 约束与现有模式

- {{业务约束}}
- {{技术约束}}
- {{现有实现模式}}
- {{数据约束、配置生效、安全、权限、事务或兼容性要求}}

## 11. 冲突、缺口与待确认项

| 优先级 | 类型 | 内容 | 影响 | 建议补证方式 |
|--------|------|------|------|--------------|
| {{优先级}} | {{冲突/漂移/缺口/待确认}} | {{内容}} | {{影响}} | {{方式}} |

## 12. 下游使用建议

{{说明 Design、Plan、Coding、Testing、Review 或 Debug 应如何消费这些上下文，不直接执行下游任务}}

## 13. 就绪状态

- 状态：{{就绪 / 有条件就绪 / 阻断}}
- 条件或阻断原因：{{没有则写“无”}}
