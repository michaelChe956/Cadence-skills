interface:
  display_name: "KnowledgeBase 初始化"
  short_description: "校验六领域输入并建立 Schema 4.0 KnowledgeBase"
  default_prompt: "使用 $knowledge-base-bootstrap 先检查既有 Manifest 和任一固定 KnowledgeBase 产物；已有产物时仅在用户显式授权重新初始化 Schema 4.0 的清理范围与风险后全新重建，再校验六领域输入、配置快照可审计范围摘要和快照标识唯一映射。"
