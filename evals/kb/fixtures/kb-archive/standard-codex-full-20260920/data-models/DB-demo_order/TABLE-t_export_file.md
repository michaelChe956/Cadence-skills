# t_export_file

## 1. 元数据

| 项目 | 内容 |
|------|------|
| 逻辑表稳定 ID | TABLE-t_export_file |
| 数据库/Schema 稳定 ID | DB-demo_order |
| 逻辑表名称 | t_export_file |
| 物理表名称或规则 | `t_export_file`（未发现物理分片规则） |
| 业务域 | 订单导出 |
| 证据基线（必填） | 1f867b9fcdf95f2e11808454898f333d7d512e90 |
| 最后核验时间（必填） | 2026-09-20 |

> `证据基线`（本次分析 Git 基线提交）与 `最后核验时间`（本次分析日期）为必填字段；为空视为文档不完整，阶段不得完成。

## 2. 业务含义

订单导出文件登记表，记录导出文件与订单的关联、文件路径与创建时间；`created_at` 注释声明“保留 7 天后清理” [代码证据]（`db/init.sql:39-40`、`order-service/src/main/java/com/demo/order/service/ExportService.java:9-10` 中 `RETENTION_DAYS = 7`）。

## 3. 字段清单

| 字段 | 类型 | 可空 | 默认值 | 主键 | 含义 | 代码映射 | 证据状态 | 证据位置 |
|------|------|------|--------|------|------|----------|----------|----------|
| `id` | `BIGINT` | 否 | DDL 未声明 | 是 | 主键 | 未发现 | DDL 已确认 | `db/init.sql:36` |
| `order_id` | `BIGINT` | 是（DDL 未声明 `NOT NULL`） | DDL 未声明 | 否 | 关联订单 | 未发现 | DDL 已确认 | `db/init.sql:37` |
| `file_path` | `VARCHAR(256)` | 是（DDL 未声明 `NOT NULL`） | DDL 未声明 | 否 | 导出文件路径 | 未发现 | DDL 已确认 | `db/init.sql:38` |
| `created_at` | `DATETIME` | 是（DDL 未声明 `NOT NULL`） | `CURRENT_TIMESTAMP` | 否 | 导出时间；保留 7 天后清理 | 未发现 | DDL 已确认 | `db/init.sql:39` |

> 证据状态只允许 `DDL 已确认`、`迁移已确认`、`代码可推导`、`用户提供`、`来源冲突`、`待确认`。缺少证据的属性单独写 `待确认`；不得依据 Entity、Mapper 或 SQL 补造实际索引、默认值或数据库约束。

## 4. 索引与约束

| 名称 | 类型 | 字段与顺序 | 定义 | 证据状态 | 证据位置 |
|------|------|------------|------|----------|----------|
| PRIMARY | 主键 | `id` | `PRIMARY KEY` | DDL 已确认 | `db/init.sql:36` |

未发现二级索引、唯一约束、外键、检查约束或触发器；`order_id` 的关联未声明外键。

## 5. 分库分表

| 项目 | 内容 | 证据状态 | 证据位置 |
|------|------|----------|----------|
| 分片键 | 未发现 | 待确认 | 未发现分片配置或路由代码 |
| 分片规则 | 未发现 | 待确认 | 同上 |
| 物理表规则 | 未发现（按单表 `t_export_file` 建模） | 待确认 | `db/init.sql:35` |
| 配置组稳定 ID | 不适用 | 已确认 | `configurations/SERVICE-order-service.md` |

> 一张逻辑表只生成本文件一份；物理分片只记录规则，不为每个物理表复制文档。

## 6. Entity、Mapper 与 SQL 映射

| 类型 | 稳定 ID 或符号 | 操作 | 字段范围 | 证据位置 | 完整性限制 |
|------|----------------|------|----------|----------|------------|
| Entity | 未发现 | - | - | 三个后端工程内未发现对应实体 | 无法从代码确认字段映射 |
| Mapper 接口 | 未发现 | - | - | 未发现引用 `t_export_file` 的 Mapper | 同上 |
| 手写 SQL | 未发现 | - | - | 三个 `*Mapper.xml` 中无该表语句 | 同上 |

## 7. 读写服务

| 服务稳定 ID | 方法或入口 | 读/写 | 调用映射 | 证据位置 |
|-------------|------------|-------|----------|----------|
| 未发现 | - | 读 | - | 未发现任何读取 `t_export_file` 的代码 |
| 未发现（推断为 SERVICE-order-service） | `ExportService`（仅保留期常量）、`ExportController.export()`（返回字面量 `taskId`） | 写（推断，未确认） | 未发现 Mapper 或 SQL | `order-service/src/main/java/com/demo/order/service/ExportService.java:5-11`、`controller/ExportController.java:10-13` |

> 上表第二行是按导出能力的合理推断，缺少持久化调用证据，不作为确认事实。

## 8. 关联 API 与页面

| 类型 | 稳定 ID | 名称 | 关系 | 证据位置 |
|------|---------|------|------|----------|
| API | 未生成（`api` 阶段尚未执行，本阶段不扫描接口） | - | - | `open-questions.md` |
| 页面 | 未生成（`pages` 阶段尚未执行，本阶段不扫描页面） | - | - | `open-questions.md` |

## 9. 特殊字段与数据规则

| 字段 | 规则类型 | 规则说明 | 证据状态 | 证据位置 |
|------|----------|----------|----------|----------|
| `created_at` | 数据保留 | 导出记录/文件保留 7 天后清理（历史 bug：曾误配 30 天，已修复回归 7 天） | 代码可推导 | `db/init.sql:39`、`order-service/src/main/java/com/demo/order/service/ExportService.java:9-10`、git 提交 `1f867b9` |
| `order_id` | 标识关联 | 关联订单（候选关联，无语义约束证据） | 待确认 | `db/init.sql:37` |

## 10. 证据与来源冲突

| 对象或属性 | 来源 A | 来源 B | 冲突或限制 | 影响 | 处理状态 |
|------------|--------|--------|------------|------|----------|
| 持久化链路 | `db/init.sql:35-40`（存在表定义） | `ExportService`/`ExportController`（无持久化调用） | 表存在但无读写代码 | 导出登记链路无法确认，保留期清理实现缺失 | 待确认（Q-M4） |
| 保留期常量 | `ExportService.java:10`（7 天） | git 历史 `1f867b9`（曾误配 30 天） | 已修复；无历史配置快照 | 当前代码值为 7 天 | 已确认（代码 + 提交信息） |

## 11. 变更记录

| 日期 | 基线 | 变更内容 | 来源 |
|------|------|----------|------|
| 2026-09-20 | 1f867b9fcdf95f2e11808454898f333d7d512e90 | 首次生成字段级文档 | knowledge-base-base-info |
